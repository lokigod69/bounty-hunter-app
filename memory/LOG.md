# Session Log
Newest first. Append-only — entries are never rewritten.
When this file exceeds ~300 lines, move the oldest half to `archive/log-2026.md`.

## 2026-07-08 (later) — 9 migrations applied to the new DB + Phase 2 leftovers shipped (invite links, persistence, mark-redeemed, orphan surface)
- **Changed:** Michael gave the go ("apply the migration and all else"). (1) **Applied the queued 6-migration batch** to the new test DB (mvbmpcmexkgfairnthux) via the IPv4 session pooler with psql — 20260412100100/100200/100300 (credit lockdown), 20260611120000 (storage buckets/policies), 20260707220000 (rejection_reason), 20260707221000 (PDF proofs). Backup taken first (schema+data pg_dump to scratchpad). (2) **Wrote + applied 3 NEW migrations** for the Phase 2 leftovers: `20260708120000` profiles.theme + onboarding_completed; `20260708120100` collected_rewards.redeemed_at + `mark_reward_redeemed(uuid,bool)` RPC; `20260708120200` invites table (owner-RLS) + `get_or_create_invite()` / `redeem_invite(text)` RPCs (redeem creates an ACCEPTED friendship, dedup either ordering). Second backup before this batch. Recorded all 9 in supabase_migrations.schema_migrations (tracker 1→10 entries). (3) **Code wiring via 3 sequential sub-agents, every diff reviewed centrally:** Phase 2.6 mode/onboarding persistence (ThemeContext + ftxGate persist to profile, fire-and-forget, localStorage stays immediate truth); Phase 2.7 deleted the orphan `/profile/edit` page (dropped its URL-exposed debug panel + un-gated mode list), folded "Restart Onboarding" into ProfileEditModal; Phase 2.8 collected-rewards "mark redeemed/undo" (domain fn + hook + RewardCard toggle + toasts); Phase 2.5 shareable invite links (new `/invite/:token` PUBLIC route + InvitePage, useInvite hook, useRedeemPendingInvite post-login round-trip via localStorage stash, Share buttons on Friends both paths + onboarding invite step). DB types added via `src/types/custom.ts` overlay (Profile.theme/onboarding_completed, CollectedRewardRow, Invite) — the UTF-16 `database.ts` left untouched.
- **Files:** 22 modified / 6 new (3 migrations, src/hooks/useInvite.ts, src/pages/InvitePage.tsx, src/domain/rewards.test.ts); 1 deleted (src/pages/ProfileEdit.tsx). i18n en+de parity for every new key (invite.*, profile.restartOnboarding*, rewards.*redeem*).
- **Verification:** `npm run build` ✓, `npm run lint` 0 errors / 3 pre-existing warnings, `npm test` 41→44 (+3 markRewardRedeemed parsing tests). **Caught a masked regression:** `npm run build` does NOT typecheck pages (root tsconfig `files: []`), so the Round-2 agent's green build hid 7 real type errors in useCollectedRewards.ts (adding `redeemed_at` to the typed `.select()` poisons the row type via SelectQueryError). Fixed with `.returns<CollectedRewardRow[]>()` + non-optional field + typed map callback. Final `tsc -p tsconfig.app.json` = 15 errors, ALL pre-existing (streaks/daily_mission_streaks/PageHeader/missions per roadmap) — 0 net new. Migration application verified live: increment RPC + user_credits writes revoked from anon/authenticated, rejection_reason present, bounty-proofs allows application/pdf, invites RLS on, 3 new RPCs granted to authenticated (auth.uid() guarded so anon-execute is harmless, matches purchase_reward convention).
- **Found:** Pre-existing i18n drift (languageSwitcher.* missing in de, friends.description missing in en) — left as out-of-scope. Post-login invite redemption for a brand-new signup fires after onboarding (useRedeemPendingInvite lives in Layout, behind FTXGate); token persists in localStorage until then, so it still redeems — acceptable for V1. NOT committed (awaiting Michael's go).
- **Open:** Michael: (a) set the new project's dashboard auth config (Site URL + redirect URLs for magic links, localhost:6075 / Vercel) so login + invite round-trip are testable; (b) browser eyeball of everything; (c) go/no-go to commit this working tree. Remaining Phase 2 is now DONE — next is Phase 3 generated assets per docs/premium-v1/ROADMAP.md.

## 2026-07-08 — Supabase revived: fresh project (Mumbai) restored from Jan-2026 backup, data wiped
- **Changed:** Restored the old cluster backup (`db_cluster-28-01-2026@04-29-25.backup`, PG 15.14 pg_dumpall, 9157 lines) into Michael's NEW Supabase project `bounty-hunter-app` (ref mvbmpcmexkgfairnthux, ap-south-1 Mumbai) — one sub-agent executed, orchestrator verified independently. Pre-flight: no role PASSWORD clauses; template1 section trimmed. Direct DB host is IPv6-only and this machine has no IPv6 route → used the Supavisor session pooler `aws-1-ap-south-1.pooler.supabase.com:5432` (user `postgres.mvbmpcmexkgfairnthux`); region discovered via `supabase projects list`. Restore produced 473 errors, all classified benign (already-exists roles/schemas, permission-denied on supabase-managed objects, realtime partition drift, vault.secrets COPY denied). Then wiped all data per Michael's explicit instruction: TRUNCATE 8 public tables RESTART IDENTITY CASCADE, DELETE 9 auth.users (identities cascaded), storage.objects already 0; 3 buckets (avatars, bounty-proofs, reward-images) kept. `.env.local` switched to the new URL + `sb_publishable_…` key; `supabase link` relinked.
- **Verification (independent):** 8 public tables at 0 rows, 3 buckets, 0 auth.users, 23 RLS policies, 14 public functions; `GET /auth/v1/health` = 200 with the publishable key. Restore log: session scratchpad `restore.log`.
- **Found:** ⚠️ **Restored schema is PRE-hardening** — `increment_user_credits` EXECUTE-granted to anon+authenticated and `user_credits` fully writable by clients (the exact top-risk the April 2026 migrations fixed); `supabase_migrations.schema_migrations` has only ONE entry (20231117000000) → old prod was managed via SQL editor, tracker useless for reconciliation. Repo migrations 20260412100100/100200/100300, 20260611120000, 20260707220000, 20260707221000 all need (re-)applying — blocked on Michael's go per the prod-SQL rule (auto-mode classifier also enforces this). `rejection_reason` column absent (expected); `proof_type` present. Edge functions and Vault secrets did not transfer (not in a DB dump / permission-denied). Old paused project still holds the only copy of real user data.
- **Open:** Michael: go/no-go for the 6-migration batch; dashboard auth config for the new project (Site URL + redirect URLs for magic links, port 6075 / Vercel); then browser eyeball of Phase 1+2 UI. Mumbai region = latency from Europe; fine for tests.

## 2026-07-07 (late night) — Phase 2 UX coherence: noun system, realtime badges, History nav, rejection loop, PDF proofs
- **Changed:** Michael decided the two parked product calls: noun system = Mission (guild) / Chore (family) / Request (couple), store items = plain Rewards, "Bounty" reserved for a mission's credit pot; proof types = PDF/text/private all allowed. Orchestrated 4 sub-agents (2 Opus, 2 Sonnet) in two waves, every diff reviewed centrally. Wave 1: (a) tasks realtime wired into useAssignedContracts/useIssuedContracts via new shared `useTasksRealtime` (debounced refetch, channel-ref guard + monotonic channel-name counter — same hardening as the UserCredits white-screen fix) + nav action badges via new count-only `useActionCounts` (review count on /issued, rejected count on /) + History nav re-enabled (route/page already worked); (b) noun-system i18n sweep en+de: new `taskStatus.*` chip keys (TaskCard hardcoded English gone), contracts/taskForm noun-neutral or `{{noun}}`-interpolated (components pass capitalized `strings.missionSingular`), rewards/bounties "Kopfgeld"→"Belohnung" sweep, `theme.guild.rewardSingular` bounty→reward (+ themes.ts fallback), dead `navigation.*`+`bounties.*` namespaces deleted, missing de `rewards.editModal` block + `contracts.createFailedMessage` key added (both latent bugs). Wave 2: (c) rejection loop — `rejectMission` now persists status='rejected' (was: silent reset to 'pending') + optional `rejection_reason`, 42703-tolerant (retries without the column until the migration is applied); reject-with-reason ModalShell dialog on IssuedPage (300-char textarea); TaskCard shows the reason to the assignee + resubmit path (uploadProof clears the reason, transition matrix rejected→review/completed added, +5 domain tests); (d) proof types — PDF+video in dropzone allowlist, uploadProof accepts PDF as proof_type 'document' (ProofType union widened), bucket migration adds application/pdf.
- **Files:** 22 changed / 8 new — key new: src/hooks/useTasksRealtime.ts, useActionCounts.ts, src/lib/debounce.ts(+test), supabase/migrations/20260707220000_add_rejection_reason.sql + 20260707221000_allow_pdf_proofs.sql (NOT applied — Supabase paused), db/proposals/009+010, docs/runbooks/SUPABASE_RESTORE_CHECKLIST.md.
- **Verification:** `npm run build` ✓, `npm run lint` 0 errors/3 pre-existing warnings, `npm test` 41/41 (33→41: +3 debounce, +5 rejection transitions). Realtime delivery NOT live-testable (Supabase paused). src/types/database.ts UTF-16LE encoding verified preserved.
- **Found:** Supabase project is PAUSED — login dead ("Failed to fetch"); no base-schema migration exists in the repo, the schema lives only in the paused project → restore checklist written (backup download first!). Private proof needs no schema work — storage RLS already limits proofs to creator+assignee. Register mix in de locale: contracts/taskForm blocks are Sie-form, rewards.empty now du-form — flag for a future one-direction sweep.
- **Open:** Michael: restore Supabase (both new migrations apply at restore), then eyeball Phase 1 glass-card surfaces + wave 1/2 UI. Remaining Phase 2: invite links (needs live Supabase), mode/onboarding persistence (prod SQL), orphan surfaces, collected-rewards loop. Phase 3 assets untouched.

## 2026-07-07 (night) — Phase 1 congruence COMPLETE: TabBar, ModalShell, accent single-source, i18n sweep
- **Changed:** Orchestrated 4 sub-agents (2 Sonnet, 1 Codex, 1 Opus) in two waves; reviewed every diff centrally. (1) Shared `TabBar` primitive (role=tablist/tab, aria-selected, roving tabIndex + arrow keys, count badges, unread dot, fullWidth variant) — Friends + RewardsStore migrated, drift resolved toward the glass aesthetic. (2) `src/theme/modeAccents.ts` is now the single TS source for mode accent hexes — modalTheme/ProfileEditModal/OnboardingStep1Mode import it; index.css keeps CSS vars with SYNC NOTE comments + 2 new drift-guard tests (suite 31→33). No drift existed between the 4 old copies. (3) i18n sweep: ~26 hardcoded strings → keys in en+de (PageState defaults, IssuedPage toasts/empty states/Fab, ArchivePage stats, UserCredits, de `rewards.confirmDialog` block added); tablist aria-labels wired. (4) Generic `ModalShell` primitive (portal, one backdrop recipe bg-black/70+blur, glass-card + modal-enter surface, Escape, UIContext modal layer, 44px close, dialog a11y, sheet/tall variants) — CreateBounty/EditBounty/TaskForm/ProofModal/ProfileEditModal migrated; TaskCard's redundant double-portal of ProofModal removed. Net −151 lines.
- **Files:** 19 changed / 3 new (src/components/ui/TabBar.tsx, ModalShell.tsx; src/theme/modeAccents.ts)
- **Verification:** `npm run build` ✓, `npm run lint` 0 errors/3 pre-existing warnings, `npm test` 33/33.
- **Found:** Intentional visual normalizations to eyeball in-browser: Create/EditBounty + ProfileEdit surfaces are now translucent glass-card (were solid gray-900); ProfileEdit backdrop /80→/70; RewardsStore tab border/text-size normalized; TaskForm/ProofModal close button moved into the surface corner. Backdrop close is now text-selection-safe everywhere. ConfirmModal deliberately NOT re-based on ModalShell (critical overlay layer + capture-phase Escape are out of shell scope). Backlog strings still hardcoded: "Submit Proof" (ProofModal), "Uploading..." (Create/EditBounty), FileUpload size error, "New User" (ProfileEditModal), MissionModalShell internals (Reward/Overdue/Delete mission/Move to History...).
- **Open:** Phase 2 UX coherence next (noun decision still parked for Michael) or Phase 3 assets.

## 2026-07-07 (late evening) - Gift emblem pilot asset generated
- **Changed:** Used the gpt-image-2 prompt skill + image generation flow to create one gift/present contract emblem pilot. Copied the generated PNG into `assets-src/generated/gift-emblem-pilot-v1.png`, then normalized the connected background field to exact solid `#FF00FF` for chroma-key use.
- **Files:** assets-src/generated/gift-emblem-pilot-v1.png
- **Verification:** PNG exists at the target path; 2,220,729 bytes; valid PNG signature; 1254x1254 square; sampled canvas edges are exact `#FF00FF`.
- **Open:** Remaining Phase 3 asset slots still pending: credit emblem, mode identity artwork, empty-state illustrations, reward-store placeholder art, iOS icon/splash.

## 2026-07-07 (evening) — Refactor landed; 4-track audit; premium polish phase 0-1
- **Changed:** Verified + committed the floating UI refactor (fixed 2 broken theme-contract tests first). Ran a 4-agent audit (design congruence / code review / UX flow / assets+sound). Fixed two white-screen P1s the audit found in the fresh refactor (emoji names crash avatarFallback; duplicate UserCredits realtime channel subscribe throws and unmounts the app). Batch A polish (dead fonts, safe areas, Spinner primitive, `.modal-enter`, `.text-sm` weight bug, sound-registry fix — 10 sound keys were silently no-oping). Asset slim-down: public/ went ~71 MB → ~2 MB (marketing/raw audio/unused fonts → assets-src/; 1 MB logo → 32 KB + 3 KB favicon). Batch B+C (2 agents): every button → AppButton, every modal → modal-enter/Escape/44px/one backdrop, PageState/EmptyState/Spinner everywhere, teal → var(--mode-accent).
- **Files:** ~45 across src/, index.html, public/→assets-src/, docs/premium-v1/ROADMAP.md
- **Commits:** 8a18540, b17469b, 2c569a3, e5543dc, b0d5a61, 3e3191d
- **Found:** UX audit: 5 nouns for the task object; Archive/History page unreachable (nav commented out); /profile/edit orphan leaks all modes + debug panel; rejected proofs vanish silently; no action badges; invites only find existing accounts; theme/onboarding localStorage-only (profile.theme is read but never written). Asset audit: coin is SVG with clean slots; is_daily/streaks have full theme strings but zero UI; no haptics. Image pipeline: Codex CLI 0.128.0 + gpt-image-2-skill (~/.codex/skills/). chrome-devtools MCP deadlocks with parallel Claude sessions (shared profile — needs --isolated).
- **Open:** dead-CSS purge + Codex image pilot running as background agents at write time; roadmap = docs/premium-v1/ROADMAP.md (5 phases); noun-system decision parked for Michael.

## 2026-07-07 — Second Brain installed; project surveyed from evidence
- **Changed:** Created `memory/` (INDEX, STATE, DECISIONS, ARCHITECTURE, LOG + raw/notes/archive) and wired the Project Memory block into new AGENTS.md/CLAUDE.md. No app code touched.
- **Files:** memory/*, AGENTS.md, CLAUDE.md
- **Commits:** none (working tree already carried an unrelated uncommitted UI refactor — left untouched)
- **Found:** Last commit 2026-06-18 "Harden V1 launch readiness" (~3 weeks idle). History: Oct 2025 doc deep-dive → security proposals 001–008 with prod runbooks → Jan 2026 onboarding/tutorial/themes burst → June 2026 codex refactor pass (domain extraction, vitest suite, credit lockdown, storage-bucket migration, fresh docs). Working tree holds an uncommitted UI-primitives consolidation (27 files, +625/−906).
- **Open:** Production migration state unknown; UI refactor unverified/uncommitted; proof-type product decision pending — see [[STATE]] Open questions.
