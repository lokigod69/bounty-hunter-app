# Session Log
Newest first. Append-only — entries are never rewritten.
When this file exceeds ~300 lines, move the oldest half to `archive/log-2026.md`.

## 2026-08-03 — Phase A begins from the LaunchOS critical path; proposal 015 corrected BEFORE applying; three stale doc lines fixed (687697c — PUSHED)
- **Context:** Michael pasted the LaunchOS NEXT_STEP block (D-022) — the ordered launch-critical path toward a TestFlight internal build. Phase A here: A1 apply 014, A2 correct-then-apply 015, A3 owner rotates the DB password, A4/A5 are the Mac/Xcode leg. He gave the explicit go for the prod SQL ("you can do the RLS switch… let's go") but did NOT paste the DB password, so the applies are staged and blocked on that one input.
- **Opened by verifying state: `8213d07`, clean, nothing unpushed, all gates matching NEXT_STEP exactly** (tsc 0, 355 tests/24 files, lint 0 errors/3 known warnings, build warning-free, bundle 401.49 kB).
- **The LaunchOS external sweep was right about 015, and the fix mattered before the apply, not after.** The draft published FOUR tables including `profiles`, because its subscription inventory said `usePartnerState.ts` watches `profiles`. Verified against the code: the hook subscribes to `friendships` (`usePartnerState.ts:149`) and derives the whole partner state machine from friendships rows — its `profiles` access is a plain fetch of the partner's display profile, not a subscription. **Nothing subscribes to `profiles`.** 015 now publishes three tables (tasks, user_credits, friendships) — publishing an unsubscribed table would have been pure WAL traffic and broadcast surface, the exact thing the up.sql's own "NOT added" note warns against. up/down/validation/runbook all corrected; the four-table version was never applied. The 014-before-015 ordering is kept (security first) even though the hard RLS dependency vanished with `profiles`.
- **Three stale doc lines fixed, all caught by the same sweep:** STATE:105 and DECISIONS:91 still showed proposal 013 as an unapplied draft (applied 2026-07-30); IOS_SHIP_RUNBOOK step 2 said the iOS floor is 14.0 "(matches Podfile)" — the Podfile and all four pbxproj entries say 15.0, raised 2026-07-24. Both memory lines now carry the correction date rather than silently reading as if they were always right.
- **Password rotation is REOPENED, and that is consistent with the old record, not a reversal of it.** The 2026-07-29 closure said: rotate if the project ever takes real users or the transcript leaves his control. TestFlight internal (Phase A5) is exactly that trigger, and the password was pasted into yet another transcript on 2026-07-30. LaunchOS A3 = Michael rotates in the dashboard AFTER 014/015 apply (rotating first would strand the applies). Never paste the new one anywhere.
- **Sequence staged for the moment the password arrives:** backup → 014 validation (expect `rls_enabled = f`) → apply 014 → re-validate (t, policies unchanged) → 015 validation (#2 empty) → apply 015 → re-validate (#2 lists three tables) → run records into the runbook. Then Michael's two-browser realtime test — the first time any live update can ever have worked on this project.
- **Gates at close:** tsc 0; 355 tests / 24 files; lint 0 errors / 3 known warnings; build pass and warning-free; bundle 401.49 kB. No src/ change this session — docs, proposals and memory only.

## 2026-07-30 — Both prod runs executed at last; the backup that protected nothing; extraction finished; mobile MAJORs closed; a formatting layer (bdabeee, 45d70af, c12c777, 0826cfb — all PUSHED)
- **Context:** Michael pasted the DB password in-session. Two sessions of "written, gated, unrunnable" cleared in minutes — which is the argument for having written them as one-command runbooks in the first place. He also asked for maximum parallelism, so three subagents ran while the DB work went on.
- **Opened by verifying state: `4a7f2a3`, nothing unpushed, clean tree, and all five gates matching NEXT_STEP exactly** (tsc 0, 240 tests/20 files, lint 0/3 warnings, build warning-free, 396.25 kB).
- **013 APPLIED.** Pre-flight showed guards absent (`f/f/f`); backup 239 KB; `BEGIN` … 3× CREATE/REVOKE/GRANT/COMMENT … `COMMIT`; post-validate showed the reward guard on create/update and the assignee guard on approve. **All three body md5s moved**, which is the difference between "it ran" and "it did something" — `approve_task` `cfc5d58d…` → `2e093f81…`. Grants intact, `increment_user_credits` still unreachable from anon/authenticated.
- **⚠️ The wipe's data backup was a lie, and the script's own guard passed it.** `backup_data.ps1` passed `--schema=public --table=auth.users` to ONE `pg_dump`. In pg_dump `--table` *narrows* the selection and `--schema` does not add the public tables back, so the dump contained `auth.users` and nothing else — no profiles, no tasks, no friendships, no invites. The guard was "at least one INSERT"; `auth.users` supplied three; it printed green. **It would have authorised an irreversible wipe against a backup holding none of the rows being destroyed** — the exact failure the file's own comments warn about, and the same shape as the 2026-07-28 batch where every prod script reported success unconditionally. Caught only because 3 INSERTs did not match the 11 rows I had just counted.
- **The fix is the general lesson: a backup guard must compare against the live database, not against itself.** Two `pg_dump` invocations concatenated at the **byte** level (`ReadAllBytes`/`WriteAllBytes`, never through PowerShell's encoding layer), and the guard now queries live row counts first and asserts per table that the dump holds exactly that many INSERTs. Verified 11/11 across 10 tables.
- **Wipe run, scope A.** tasks 2→0, friendships 2→0, invites 1→0, one stale `partner_user_id` nulled, profiles and `auth.users` kept at 3. Scope B was **declined in favour of a third option** — scope A plus deleting a single auth user by hand — which gets Michael the signup/onboarding re-test he wanted without the one-way door, and leaves two working accounts as a way back in if confirmation email is misconfigured.
- **Correction to the record: `tesatmynutes` was never a self-assigned contract.** Its `created_by` was cryptobonobo and its `assigned_to` was the gmail account — two different users — and its reward type was `text`, not `credit`. That is why validation #5–#7 returned zero rows *before* any wipe. The self-assign hole was real as a **capability in the function bodies** but had never been exercised on this database. Both the runbook and STATE said otherwise; both corrected.
- **`data_backup_*.sql` is now gitignored.** It dumps `auth.users` — real emails, `encrypted_password`, `recovery_token`. `schema_backup_*.sql` stays tracked (schema-only, no rows), which is the existing precedent.
- **Extraction finished: 93 keys × 12 locales** (466 → 559 leaves each). EmojiPicker's 20 tooltips, FriendCard, FriendSelector, the shared reward image field, Layout aria-labels, `themes.ts` mode label/description, the RPC error table. `label`/`description` were **removed from `ThemeDefinition`** rather than translated in place, so they cannot be re-hardcoded — and they now follow a language switch instead of being frozen at module load. `missions.ts` keeps its no-i18next rule: the code→key map lives in a new UI-layer `taskLifecycleErrors.ts`, which generalised what had been a 013-only special case, so every lifecycle code localizes now.
- **Three mobile MAJORs closed, and two of them needed no breakpoint at all.** Portrait header: the left group had `min-w-0` so it yielded, but its child was `flex-shrink-0` and so did not yield with it — **an unshrinkable child does not shrink, it overflows its parent's box** and paints over the next sibling. 474.7px demanded of 358px; 117px of wordmark landed on the sigil and credit pill, concealed by `body{overflow-x:hidden}` exactly as the landscape BLOCKER was. FriendCard: a flex item's `min-width` defaults to `auto` (= min-content), and the action cluster is unbreakable 44/48px buttons, so it could not shrink at all while `min-w-0` on the identity column donated an already-insufficient budget. TaskCard: the `h3` was the only box with both `flex-shrink:1` and `min-width:0`, so it absorbed the whole deficit, and `line-clamp-2` is `overflow:hidden` — **a zero-width title renders nothing**, which is why the symptom was a missing title rather than a clipped one.
- **The inverse of the landscape lesson, worth keeping:** the narrowest task card in the app is the **desktop** `lg:grid-cols-3` one at 264px, not a phone at 296px. Sizing the German fix to the phone would have fixed the report and left desktop broken. The smallest container is not on the smallest viewport.
- **15 new mobile guards, 10 confirmed failing on the pre-fix markup first.** The two that pass by design are renamed `RATCHET (passes pre-fix): …` so nobody mistakes them for regression probes. An optional third ratchet was dropped rather than bake a ±5% font-metric estimate into CI where a translation edit could trip it on a guess.
- **A formatting layer, on the `missions.ts` rule:** `src/i18n/format.ts` takes locale as its **first argument** and never reads the active language, so plain modules stay pure; `useFormatters()` binds it for components. It depends on `useTranslation()` deliberately — that subscription is what re-renders numbers on a language switch, where reading the singleton would be right on first paint and stale forever after.
- **CLDR does not abbreviate thousands in every language.** The hand-rolled `${(v/1000).toFixed(1)}k` was wrong twice (English "k"; a "." decimal point in eleven of twelve locales that write "1,2") — but German and Italian short-compact notation does **not abbreviate thousands at all**: 12000 is "12.000", and abbreviation starts at millions. A test pins it, because it reads as a bug until you check CLDR. My first test asserted the wrong thing and failed correctly.
- **`src/utils/dateUtils.ts` deleted.** `getStartOfWeek` hard-coded Sunday-first with a comment citing "typical US calendar" while eleven of twelve locales are ISO Monday-first — and it had **zero importers**. Deleting removed two of the thirteen known formatter defects and a landmine, instead of building a `getWeekInfo` WebKit fallback for dead code.
- **Real mojibake found in source, not just locale JSON.** `RewardsStorePage.tsx` held `Â·` (U+00C2 U+00B7) where `·` belonged — the UTF-8 bytes of a middle dot decoded as Latin-1 — so users read "Lifetime earned Â· 1,234". An em dash in a nearby comment was corrupted the same way. Found by scanning **all** of `src/` for the signature; only those two. The `.tsx` surface was never being checked, only the JSON.
- **Having DB access paid for itself twice over: two defects found that are invisible from the repo.** Both written up as proposals 014/015 and left unapplied per the prod-SQL rule.
  - **`public.profiles` has four RLS policies and RLS switched OFF.** All four are inert, and `anon`/`authenticated` hold DELETE/INSERT/UPDATE/TRUNCATE. **The anon key ships in the deployed JS bundle**, so anyone who reads it out can rewrite or truncate every profile. `tasks`, `friendships` and `user_credits` all have RLS on — profiles is the only one that does not, and no migration in the repo ever enabled it. The fix is one line; every client write path was checked against the existing policies first, and there is no DELETE call site in `src/` at all.
  - **`supabase_realtime` contains ZERO tables** (`puballtables = false`), so Postgres emits no change events and **all five** `postgres_changes` subscriptions are silently inert — tasks ×2, `user_credits`, `friendships`, partner state. Nothing errors; the symptom is "the other browser didn't update", which reads as a caching bug. **This is the real answer to the parked question about `friendships` and the nav badge** — the question assumed one table was missing; nothing is published at all. Almost certainly 2026-07-08 migration fallout, like the auth Site URL and the edge functions.
  - Ordering matters: 014 before 015, because Realtime applies a table's RLS to decide who receives a row, so publishing `profiles` while unprotected would broadcast profile changes to everyone.
- **Also answered from the DB, both parked for weeks:** all three storage buckets are **already empty** (0 objects), so the wipe runbook's manual step 3 is a no-op; and the realtime question above.
- **Codex adversarial review of the extraction commit found one MAJOR the gates could not**: six lifecycle-error paths in `Dashboard.tsx` and `IssuedPage.tsx` still render `error.message`, which since the refactor is only the generic per-operation fallback — so those paths got **less** specific in English too, not just untranslated. Plus two newly-added reward-image keys that are unreachable because `validation.error || t(...)` always short-circuits on a truthy English string. Categories 1 and 3 (the `themes.ts` property removal and interpolation parity across twelve locales) came back clean.
- **Gates at close:** tsc 0; tests 240 → **312** (22 files); lint 0 errors / 3 known warnings; build pass and warning-free; main bundle 396.25 → 401.26 kB.

## 2026-07-29 (later) — All three decisions answered and shipped; mobile BLOCKER closed; onboarding i18n'd into twelve languages; prod runbooks written but unrunnable (d223535, d1d243b, eed7cc0, 2412cb0, d5dedb8 — all PUSHED)
- **Context:** Michael answered everything at once — "Yes 013 go everything else as well german make it not formal ... push everything and wipe everything". So: all three parked decisions accepted, plus the mobile pass, plus the extraction, plus a prod data wipe.
- **Session opened by verifying state and pushing the six stranded commits.** All four gates matched NEXT_STEP exactly (tsc 0, 198 tests/19 files, lint 0 errors/3 warnings, clean tree). `140b90e..c6baa48` — the 2026-07-29 morning work had genuinely never left the machine.
- **⚠️ PROD IS UNREACHABLE FROM THIS AGENT.** The DB password is not in the environment, not in the repo, and the two routes to find out (`Get-Content .env.local`, `supabase projects list`) were both blocked by the permission classifier. So 013's SQL and the wipe could not be executed. Everything else was done; those two were written as one-command runbooks instead. **This is likely to recur every session** — plan for Michael running prod SQL himself.
- **Rank ladder recalibrated (Michael's decision 2):** 0/120/600/2000/8000 → **0/30/150/600/2000**. Bands now land at roughly 10/50/200/667 contracts at the ~3-credit rate the form actually offers. THE_REGISTER's band table and its open question #1 both updated rather than left contradicting the code. A test pins the top band as reachable at ≤700 contracts, so the table cannot silently drift back.
- **013 approved and its client half shipped** (SQL still Michael-gated). New `TaskLifecycleRpcError` carries the machine-readable RPC code alongside the English fallback, which is what lets one rule be localized without the domain layer taking an i18n dependency — `IssuedPage` used to rebuild the error and throw the code away, so it now re-throws lifecycle errors unwrapped. `approveMission` returns `{credited, creditSkippedReason}` and the approval toast says out loud when nothing was paid. **The load-bearing detail: a pre-013 server omits `credited`, and absence is read as PAID** — the old server always paid, so the opposite default would have told every user their credits vanished in the window before the SQL lands. Six tests, including that case.
- **Open point E answered from the code, not by asking again.** No path in the current tree can create a self-assigned contract: the dropdown is friends-only and `validateForm` requires a selection; a self-friendship would put you in your own dropdown (`useFriends.ts:82` takes "the other side" of the pair, which for a self-pair is you) but both writers refuse one — `sendFriendRequest` rejects `userData.id === userId` and `redeem_invite` returns `SELF_INVITE`; `create_task` does **not** default a null `p_assigned_to` to `auth.uid()`; onboarding step 4 has created nothing since R35; the seed makes Alice→Bob. **Conclusion: the row predates 012 making creation RPC-authoritative (2026-07-28), when the client inserted into `tasks` directly under a policy that constrained only `created_by`.** Nothing blocks the apply.
- **German is now informal (decision 3), and it was far smaller than estimated: 29 strings, not ~410.** The file was already ~93% "du"; the formal remnant sat in the older surfaces (contract errors, friends, profile, task form, the onboarding mode intro). Converted by exact-match replacement rather than a regex over "Sie", which would have rewritten lowercase "sie" (she/they) into nonsense — e.g. "Erledige eine Aufgabe und **sie** wird hier gespeichert". **The register guard was probed against 21 cases before being trusted**: all 10 replaced strings caught, all 11 informal ones quiet.
- **Mobile BLOCKER root cause and fix.** `md:` is width-only and a landscape iPhone is 844×390 / 932×430, so it cleared 768px and took the desktop header. New `nav` screen at `(min-width:768px) and (min-height:500px)` — above every phone landscape height (~430px ceiling) and below iPad landscape (768px). **The same width-only mistake was making three other bugs**: `.input-field`'s 16px/44px floor was guarded by `sm:` (640px), so a landscape phone took the *desktop* branch and dropped to 14px/40px on precisely the device where iOS focus-zoom bites — re-guarded on `(pointer: coarse)`.
- **`dvh` alone would NOT have fixed the keyboard bug.** iOS does not shrink the layout viewport when the keyboard opens, so a `90dvh` modal keeps full height and the keys cover the submit button. Modal heights are now a three-line ladder: plain `vh` (the floor — the build targets iOS 15.0 but `dvh` landed in 15.4), then `dvh` (tracks the collapsing URL bar), then `min(dvh, --visual-vh * n)` where `--visual-vh` comes from a new `useVisualViewport()` reading `window.visualViewport`.
- **`.safe-top` was silently replacing `pt-16`, not adding to it** — it is unlayered CSS and `pt-16` is a `@layer utilities`, so unlayered wins outright and the mobile menu panel cleared the notch but not the header (zero clearance on a notchless Android). `.safe-top` is now additive via `--safe-top-extra`.
- **Doubled gutters**: Layout's `<main>` had `container mx-auto px-4 py-6` and every page's `PageContainer` adds its own — 296px usable at 360px. Verified all five pages under the outlet root in `PageContainer` (including loading and error branches) before making `<main>` gutterless.
- **TabBar's first tab was unreachable**, and the reason is worth remembering: `justify-center` on an `overflow-x-auto` container pushes overflow to **both** sides, and content past the start edge cannot be scrolled to because `scrollLeft` bottoms out at 0. `mx-auto` on the inner tablist centres when there is room and resolves to 0 when there is not. German triggered it; any long enough label pair would.
- **9 mobile guards, each probed against the pre-fix source shape first (11/11 probes correct)** — a guard that has never been seen to fail is decoration. The emitted CSS was also grepped directly for both new media queries rather than assumed.
- **Onboarding extracted and translated into all twelve locales in one commit** (51 keys × 12). It had to be atomic: the parity gate means English-only keys break the suite. Step titles are now keyed by name, not looked up out of two parallel arrays by index. Mode nouns stay in ThemeStrings and are passed as **interpolation values**, not concatenated — which is what makes the translations grammatical, since several languages need the noun in a different case or position. New guard pins the placeholder *vocabulary* to what the component supplies, because the existing test only proves locales agree with each other and would pass happily while every user read a literal `{{mission}}`.
- **Two prod runbooks written, gated, and left for Michael.** `PROD_RUNBOOK_013.md`, and `PROD_RUNBOOK_WIPE_TEST_DATA.md` with `db/maintenance/*.sql` + two scripts. The wipe needed a **data** backup script that did not exist — `backup_schema.ps1` is `--schema-only`, which before a data wipe is worse than no backup because it succeeds and authorises the run while containing none of the rows at risk. Three gates on the wipe (PROD_CONFIRM, WIPE_CONFIRM = the project ref, and a >1 KB data backup from today); **all four failure paths were exercised and confirmed to block** without touching the network.
- **Two scopes offered for "wipe everything", because only one is recoverable.** Scope A (default, recommended) clears app data and keeps accounts; scope B also deletes profiles and `auth.users`, which forces re-registration — and re-registration depends on dashboard auth config that is not in this repo and was wrong as recently as 2026-07-11 (Site URL still `localhost:3000`). A public-schema restore does not bring auth accounts back.
- **Self-inflicted and worth remembering:** used `Set-Content -Encoding utf8` on `RewardsStorePage.tsx` and added a BOM — the exact PowerShell trap this project already documents for Codex. Caught by checking the bytes immediately after; stripped with Node. **Never write project text files through PowerShell.**
- **Gates at close:** tsc 0; tests 198 → **240** (20 files); lint 0 errors / 3 known warnings; build pass and warning-free; main bundle 396.25 kB.

## 2026-07-29 — Michael's browser walkthrough triaged: 5 design fixes, proposal 013 drafted + proven locally, i18n rearchitected for 12 languages (24a7613, 7831ff9, 9620413)
- **Context:** Michael did a live two-account walkthrough and dictated a long stream of feedback with screenshots, plus three standing decisions: **do not rotate the DB password** (risk accepted — it stays), **close the self-assign standing hole**, and **add ~10 European languages, Romance first**, using Codex heavily.
- **013 DRAFTED + PROVEN LOCALLY (24a7613, unapplied).** The hole: THE REGISTER §2 claims standing "cannot be self-awarded (approval requires `created_by = auth.uid()` and pays `assigned_to`)" — but those conditions are not in tension when one person is both, so a self-assigned contract mints unlimited `total_earned`. Three body swaps, no signatures/policies/schema changed → no types regen, no deploy-ordering window: `create_task`/`update_task` reject a credit reward on a self-assigned contract (`update_task` on the **effective post-patch** values, closing the "create for a friend then re-point to self" walk-around), and **`approve_task` V4 refuses to credit when `assigned_to = created_by`** — the actual guarantee, since it is the only function that can reach `increment_user_credits`. Self-assigned contracts still complete (stranding one in `review` forever is worse than a silent non-payment); the response gains additive `credited` / `credit_skipped_reason` keys.
- **Verified without touching prod:** spun a throwaway Postgres cluster with the local `initdb`/`pg_ctl`, built a stub schema plus a switchable `auth.uid()`, applied harness → 012 → 013 clean, then ran **20 assertions, all PASS** — including the core one: a pre-existing self-assigned credit row (inserted directly, i.e. a pre-013 row) completes with `credited=false` and `total_earned` **unchanged**, while a real contract still pays the hunter 25. Also proved the rollback restores the 012/V3 bodies (hole re-opens, as documented) and that re-applying is idempotent. Cluster destroyed afterwards.
- **Open point E, found while drafting:** `TaskForm.tsx:283` populates the assignee dropdown from **friends only**, yet a live self-assigned contract exists (Michael's `tesatmynutes`, creator and assignee both `laqy69`) — so some other path creates them. Validation query #7 finds it before apply.
- **Five design fixes from the walkthrough (7831ff9).** (a) Modal scrim was hard-coded **five different ways** (`black/70` x3, `/90`, `/60`) — the same gesture dimmed the page by a different amount depending on which modal opened; now one `--modal-scrim` at 0.82 (plus `-heavy` 0.94 for the lightbox), fully opaque under reduced-transparency where the blur is gone. (b) **Root-caused the empty-column bug:** `children` is a JSX expression *list*, so React passes an ARRAY — `[false,false]` when every branch is falsy — and an array is truthy, so `!(description || children)` was never true; a contract with no description therefore rendered an empty left column and shoved the reward to the far right. `React.Children.toArray` asks the honest question. (c) Description and reward panels are now always equal height (`items-stretch`, no `self-start`), with short text centred via `my-auto` rather than `align-items:center`, which clips the first line in a scroll box. (d) German header collision (UNGESCHWOREN over Verlauf): neither header group had `min-w-0`, so with nowrap nav items and a nowrap rank word nothing could shrink; added it, and the rank **word** now renders only at `xl+` with the sigil carrying standing below that — deliberately width-robust, since the rank word is a translated noun and the Romance locales run longer than German. (e) A segmented control with one option is a question with one answer: the profile mode switcher is hidden unless >1 public mode exists, and onboarding step 1 keeps its hero art but drops the choice framing. Both auto-revert if `PUBLIC_THEME_IDS` grows.
- **i18n rearchitected (9620413).** Twelve statically-imported locales would have added ~300 kB to the main bundle, so locales are now one lazy chunk each (`import.meta.glob`), with English alone eager because it is the fallback — German moving out already dropped main 407.8 → 390.5 kB. **Dropped `i18next-browser-languagedetector`** (and its vite `manualChunks` entry, which broke the build until removed): it hands i18next a language whose chunk has not been fetched, causing a visible English-then-swap first paint. The loader reads the same `i18nextLng` key the detector used (so existing devices keep their choice), clamps region tags (`pt-BR` → `pt`), and awaits the chunk before init; `main.tsx` awaits `i18nReady` and mounts anyway on failure. `AVAILABLE_LANGUAGE_CODES` is **derived from the filesystem**, not declared, so a language can never appear in the picker and then silently render English. Languages are named by endonym.
- **New `src/i18n/languages.test.ts` (17 tests)** locks what fails silently in a browser: key parity against English, placeholder preservation, no empty leaves, no duplicate keys, no BOM, no U+FFFD — and it checks every locale present on disk, so incoming languages are gated automatically. It immediately found two real defects: **en and de both carried a duplicate `contracts.reject`** (a string shadowed by the reject-dialog object — harmless today only because nothing reads the string form), and de's orphan **`friends.description`**, which was the long-standing 410-vs-409 key discrepancy and used informal "du" against the file's formal "Sie". Both removed; en/de now at exact parity.
- **Codex delegated four ways** (all read-only except the translations): adversarial mobile audit, hard-coded-string audit, and two translation waves (es/pt/it/fr/ro and nl/pl/sv/da/cs).
  - **Mobile audit: 11+ findings, 1 BLOCKER** — at 844x390 (landscape phone) the full desktop header renders with the hamburger hidden and controls overlapping/clipped, because Tailwind's `md` is width-only and `body{overflow-x:hidden}` conceals the overflow. MAJORs: portrait header logo overlaps standing/credits; form modals are not visual-viewport aware (`vh` not `dvh`, so the iOS keyboard covers submit); `.input-field` drops to 14px/40px in landscape → iOS focus zoom plus sub-44px targets; `.safe-top` *replaces* `pt-16` instead of adding to it; doubled 16px gutters (main + PageContainer) leave only ~296px usable at 360px; the reward grid starts at `grid-cols-2` and clips Edit/Delete; the centred-and-scrollable German tab bar puts the first tab before the scroll origin where it cannot be reached; FriendCard action rows overflow; German daily task cards lose the title entirely.
  - **String audit: ~150 hard-coded user-visible English strings** across onboarding (the whole flow — there was no `onboarding` namespace at all), the CreateBounty/EditBounty modals, EmojiPicker (20 tooltips), FriendCard, FriendSelector and Layout aria-labels, plus 13 locale-unaware formatters (`toLocaleDateString(undefined, …)`, a hard-coded `k` suffix, `Intl` never given the active language, `dateUtils` assuming Sunday-first) and 5 concatenated/ternary pluralisations. It confirmed the nested `strings` in `themes.ts` are safely overridden by `useThemeStrings`, **but** the top-level `label`/`description` on all three modes render directly and are untranslated. It independently reproduced the `friends.description` orphan.
- **Gates after every batch:** tsc 0; tests 110 → **127** (19 files); lint 0 errors / 3 known warnings; build pass **and warning-free** (`en` had to be excluded from the locale glob to keep it so).
- **Answered a standing open question with data instead of asking again:** `standing.domain.ts` thresholds (0/120/600/2000/8000) assume "roughly 20 credits per contract" in their own comment, but `TaskForm` offers **1/2/3/5/10** and labels 2 as "small chore". At a realistic ~3 credits per contract, band 3 is ~667 contracts and band 4 ~2,667 — effectively unreachable. Recommend dividing the table by ~4 (0/30/150/600/2000). Michael-gated, because it retroactively promotes existing ranks.
- **In flight at save time:** both Codex translation waves writing ten locale directories. They write JSON via PowerShell, which is exactly the UTF-8 round-trip trap, so the new BOM / U+FFFD / parity tests are the acceptance gate.

## 2026-07-28 late → 2026-07-29 early — Proposal 012 APPLIED to prod + a false-success bug found in every prod script (1096190 @ 07-28 23:29, e68d618 + 44e4d89 @ 07-29 01:46)
- **Michael's own rollout attempt silently did nothing.** His terminal printed `Backup written: …232206.sql` and `Migration applied successfully` in green — but psql never authenticated, the backup file did not exist, and the DB was untouched. **Root cause: not one of the 12 scripts in `scripts/prod/` checked `$LASTEXITCODE`;** the green success line was unconditional. Worst instance was `rollback_012.ps1` (would report a rollback that never happened, while prod stayed broken). Fixed all 12 in `1096190`: exit-code capture + throw with messages stating what did NOT happen; `backup_schema` now also asserts the dump file exists and is >50 KB; `apply_003`/`apply_008`/`rollback_003` gained the missing `-v ON_ERROR_STOP=1`. Proven by running validate with a deliberately wrong password → now throws `VALIDATION FAILED (psql exit 2)` where it previously printed green.
- **Rollout executed centrally** (Michael explicitly granted permission after the interactive password prompt defeated him — the prompt is a SecureString, so pasting shows nothing; setting `$env:PGPASSWORD` first skips it entirely): pre-flight validate clean → real backup `supabase/schema_backup_20260728_232501.sql` (236 KB, 314 CREATE stmts, verified + committed) → apply in one transaction (BEGIN → 2 CREATE FUNCTION + REVOKE/GRANT/COMMENT each → 3 DROP POLICY → COMMIT) → post-validate.
- **Post-state confirmed:** `create_task`/`update_task` live, both SECURITY DEFINER with `search_path=public`; **`tasks` now has ZERO client write policies** (only `View tasks`, `Users can view assigned tasks`, `Admins full access`); all 8 editable columns intact; the six 011 RPCs still live. `anon` retains EXECUTE — the same benign Supabase platform default recorded for 011; every RPC checks `auth.uid()` first. Smoke-tested both RPCs unauthenticated via psql: each returned `{"success": false, "error": "not_authenticated"}` and wrote 0 rows.
- **Client caught up (e68d618):** `database.ts` regenerated from the live project (+42 lines, 0 deletions — purely the 2 RPCs; encoding still UTF-8 BOM/CRLF, no UTF-16 regression); the temporary `DatabaseWithTaskMutationRpcs` overlay deleted and all 4 consumers moved to `SupabaseClient<Database>` (mirrors the 011 cleanup); optional RPC args now pass `undefined` not `null` (generated types model omission as undefined — both marshal to SQL NULL), with `p_deadline` keeping `||` so `''` still collapses (Postgres cannot cast `''` to date). Gates: tsc 0, 110/110, lint 0 err, build pass.
- **Open:** Michael must rotate the DB password (shared in chat). Browser create/edit test still unperformed.

## 2026-07-28 (later) — Codex session review CLOSED: 8/8 findings fixed (8e58cf4)
- Codex read-only adversarial review of the whole session diff (eb98b2c..8e58cf4's parent) returned **8 findings, 0 BLOCKERs**; five focus areas fully clean (theme-string parity, 012 RPC arg mapping/whitelist, edge-function escaping/auth, CSS/z-index/reduced-motion, Phase B envelopes). All 8 arbitrated as real and fixed in `8e58cf4`: (MAJOR) credits state now identity-tagged (`dataUserId`) so a session swap can never render or record account A's standing as B's — watcher keys off the data's owner; (MAJOR) useDailyQuote localStorage fully guarded (SecurityError/quota could previously throw a cosmetic footer quote into the root ErrorBoundary — pre-existing since v4); (MINOR×6) in-memory last-band map kills setItem-failure re-fires, reduced-motion ceremony hold now 4.5s (was 1s SHORTER than normal — wrong direction), NaN index validation, per-user deck namespacing, Set-uniqueness deck check, Dashboard credits fetch deduped through useStanding. Full provider-level credits dedupe deferred (still ~4 listeners on CREDITS_CHANGED — harmless at household scale). Gates: tsc 0, 110/110, lint 0 err, build pass.

## 2026-07-28 — Phase B client MERGED to main; edge functions hardened; Design V2 Wave 2 (Standing) SHIPPED (7b7414e, 4803957, 208687a, 80a7d77)
- **Context:** Michael skipped the browser smoke ("no users, push freely to main, break prod if needed"), and ordered: rebase/merge the 012 client branch, harden the Gmail edge functions, and build deferred items toward a congruent finished state. Codex reserved for adversarial review; Claude arbitrates.
- **Phase B client on main (7b7414e):** `phase-b-client-rpcs` rebased onto Design V2 main — clean auto-merge, RPC arg shapes verified against 012 up.sql (8 create args, 8-column patch whitelist). ⚠️ **Deployed create/edit is BROKEN until Michael runs the 012 SQL** (RPCs don't exist on live yet) — explicitly accepted. Rollout = validate_012 → backup_schema → apply_012_up → post-validate → types regen (`supabase gen types --project-id mvbmpcmexkgfairnthux`).
- **Edge hardening (4803957):** verified via `supabase functions list` that ZERO functions are deployed on mvbmpcmexkgfairnthux — the "legacy Gmail" risk was the old project; repo-side: `notify-reward-creator` got escapeHtml on reward/display names (stored-HTML-injection into Resend email), dead `create-daily-tasks` (targets nonexistent `recurring_contract_*` tables) became a fail-closed 401/410 stub, security tests extended (11 pass). Client's `functions.invoke` is fire-and-forget-with-catch, so the missing deploy never breaks purchases.
- **Wave 2 Standing (208687a + 80a7d77):** THE REGISTER §3.1/§4.9, zero-migration. `standing.domain.ts` (5 bands 0/120/600/2000/8000, progress, creed unlock 1/2/4/6/7; thresholds in ONE table) + 11 tests; CSS hexagon `StandingSigil` (hollow → iron → iron+chevrons → brass → mode accent); `StandingBlock` in header (desktop full / mobile compact, UNSWORN shows no bar + recruitment tooltip); `RankUpCeremony` mounted in Layout (dim 220ms, 96px strike on new `--ease-metal`, ring 1→3.2, rank-word wipe, reduced-motion 2.5s cross-fade), `feedback.rankUp()` = Heavy + payday@140 + success@320 (seal.mp3 still uncommissioned → strike is haptic-only, fake-timer contract test); `useRankUpWatcher` baseline-first per-device per-user (never replays); THE NAMED styles OWN name only (others' standing unreadable — `total_earned` lives on RLS-bound `user_credits`, cross-user needs a migration); creed deck now drawn from band-unlocked pool (v5 storage keys, undefined-while-loading keeps the board foot silent); rank words are ThemeStrings ×3 modes ×en/de (parity test extended to 41 keys). Fixed own review finding: `known` settles once so StandingBlock never unmounts on CREDITS_CHANGED refetch (Wave 0 law).
- **Also:** Friends empty-state hardcoded English → i18n (invite.* keys en/de); docs/overview.md + open-questions.md stamped HISTORICAL; README checked (already accurate).
- **Verification:** tsc 0; Vitest 18 files / 110 tests; lint 0 errors / 3 pre-existing warnings; build pass — re-run after every batch. All pushed; Vercel deploys.
- **In flight at save time:** Codex read-only adversarial review of `eb98b2c..HEAD` (task-ms5amb42-hz99r3); findings to be arbitrated + fixed next.

## 2026-07-24 (later) — Design V2 SHIPPED: waves 0/B/C committed + Codex adversarial review fixes (46454d3, ad447b4, 4910a08, 0ebaf5c)
- **Context:** Michael green-lit implementing THE_REGISTER with the instruction "you and codex decide together, codex executes, then codex adversarially reviews." Pipeline ran exactly that way: 3 Explore verification agents re-checked every load-bearing doc claim (doc held up: ~80% confirmed exactly; notable corrections — the starfield never existed so it was written fresh, not restored; mode-accent system was 2 competing accents not 1 dead one; Vite targets Safari 14 so the white-screen claim was false, the real bug was Podfile 14.0 vs Capacitor 8's 15.0 minimum) → Codex reviewed the design doc (verdict: real direction, but absolute rank bands contradict arbitrary household economies, guild-rate tiers need a household entity that doesn't exist, standing is gameable via self-assignment — creation never checks the assignee relationship, so "cannot be self-awarded" is FALSE) → Wave 0 implemented centrally, Waves B/C by Codex write-mode, each gate-checked + committed → Codex read-only adversarial review of the full diff (9 findings: 1 BLOCKER, 7 SHOULD-FIX, 1 suspicion) → all 9 fixed centrally (0ebaf5c).
- **Review-fix batch (0ebaf5c):** cross-user stale-state BLOCKER (all 4 SWR hooks: synchronous clear on identity change + request-epoch so only the newest request commits; background failures keep populated lists); usePayoutWatcher serialized + processed-ledger + hidden-payout queue delivered on foreground; credit readers epoch-guarded; Seal removeSeal stabilized; Escape ignores key auto-repeat; starfield made fully static (the drift violated the pass's own motion law + idle WKWebView paint); Safari audio unlock (muted play inside first gesture).
- **Sound default CHANGED: fresh installs are sound OFF / haptics ON** (two-reviewer consensus; existing devices keep their stored pref). If a wiped device seems "silent", that's the new default — the profile toggle turns sound on. Haptics got its own toggle next to it.
- **Mandalore glyph specimen** (open question #6) published as an artifact for Michael's eyeball: https://claude.ai/code/artifact/21234c79-d050-4a84-99eb-94afa910dcf2 — real TTFs at 22→9px, rank words, digits vs system tabular, German umlauts.
- **Deferred with reasons (per joint Codex ruling):** rank bands/ceremony + sigil (needs owner-gated migration for monotonic rate-normalized points — absolute thresholds are wrong for arbitrary credit economies), guild-rate tiers (no household entity to compute a median over), swipe-to-execute approval (irreversible mint behind an accidental gesture), THE DROP's full 5-coin parabola, bottom-nav chassis, presence/wear/callsigns/stamped-ledger, audio re-encode (needs ffmpeg pass), self-hosted Poppins woff2, credit_transactions earn-insert migration (Michael-gated via db/proposals). react-router advisories (2 moderate) need a v7 major — deferred. Route-lazy stays rejected per 2026-07-14 decision.
- **Verification (all four gates re-run centrally after every wave and after the fix batch):** tsc 0 errors, 93/93 tests (17 files), lint 0 err/3 pre-existing warnings, build clean. Pushed to origin/main → Vercel test env deploys.

## 2026-07-24 — Design V2 Wave C implemented (uncommitted)
- **Commitment language:** new route-persistent `SealLayer` + event bus gives accept, proof transmit, approval, and sent-back actions one anchored 44px mark. Strike seals use `--ease-press`, a delayed same-colour ring, landing haptic, reduced-motion fade, 3-seal cap, and auto-removal; rejection uses the orange lift inversion with no shock/haptic. The requested blur was represented by a transform/opacity ghost ring to honor the wave's hard transform/opacity-only constraint.
- **Correct payday address:** `usePayoutWatcher` is mounted once in Layout, baselines on first fetch/login, diffs only hunter-side `review → completed` credit rows, stays quiet while hidden, then emits payday + a one-coin header drop on the hunter device. `bh:credits-changed` refreshes all header/mobile credit widgets and the shared credits hook. Four pure diff tests added.
- **Standing/login/creed:** both credit readers now select `total_earned`; Rewards Store renders a quiet localized lifetime-earned line only for numeric data. Login uses fixed vignetted guild key art and a dark Google surface with the official four-colour mark. Daily quotes now serve only the seven bilingual in-world creed lines through i18n; German creed resource added and the 113 generic quote module deleted.
- **Constraints:** no SQL/dependencies; protected `useTasks.ts`, `types/custom.ts`, `domain/missions.ts`, and Issued create/update block untouched. All new motion is transform/opacity with explicit reduced-motion paths and no idle loops.
- **Verification:** `npx tsc -p tsconfig.app.json --noEmit` 0 errors; Vitest 17 files / 93 tests; lint 0 errors / 3 pre-existing fast-refresh warnings; production build passed. Browser/device smoke and intentional Wave C commit remain for Michael.

## 2026-07-24 — Design V2 Wave B implemented (uncommitted)
- **Lifecycle truth:** `rejected` now maps to its own orange `ModalState` and localized "Sent back" label; Issued gained a non-empty-only sent-back section. Pending assignee cards now expose **Accept contract** → existing `set_task_status` client path, with `feedback.press('acceptContract')`; proof submission remains gated behind `in_progress`, and rejected keeps resubmit.
- **Evidence:** new `EvidencePanel` renders `proof_description` plus signed private image/video/PDF evidence for creator and assignee across review/completed/rejected. The old TaskCard-only signed-link logic moved to `useSignedProofUrl`; stored URL format is unchanged and failed inline media falls back to an external signed link.
- **Stability/accessibility/history:** assigned/issued/archived/friends hooks now stale-while-revalidate with `isRefreshing`; populated page lists no longer swap to spinners. Both modal shells share focus move/restore + a Tab trap; Escape handlers are LIFO (ConfirmModal and reward lightbox migrated); ConfirmModal uses modal material + entry animation. Archive queries assigned **or** created rows, orders by `approved_at` then `created_at`, and settled cards hide countdown alarms. Dashboard duplicate stats removed (completed header counts the unsliced source); reward claim feedback changed payday → success.
- **Constraints:** did not touch `useTasks.ts`, `types/custom.ts`, `domain/missions.ts`, or IssuedPage's protected create/update block. No SQL or production changes.
- **Verification:** `npx tsc -p tsconfig.app.json --noEmit` 0 errors; Vitest 16 files / 89 tests; lint 0 errors / 3 pre-existing fast-refresh warnings; production build passed. Added `src/theme/modalTheme.test.ts` for rejected mapping + orange token. Browser/device smoke remains for Michael.

## 2026-07-24 — SIDE-QUEST (outside protocol-os): adversarial design/UX review + V2 direction "THE REGISTER" (docs only, zero code)
- **Context:** Michael asked for an adversarial review of style/design/frontend/UI/UX and a bold immersive redesign proposal, explicitly framed as a side-quest rather than a workstream resume. Ultracode: 27-agent workflow `wf_4294ddcc-7ce` — 7 forensic recon passes, 7 design-director critiques, 5 competing design directions, 1 principal-engineer stack audit, 3-lens judge panel (taste / daily-use / memorability). 23 completed; the 3 feasibility agents + master-synthesis agent died on a session limit, so the synthesis was written centrally instead.
- **Outcome — `docs/design-v2/THE_REGISTER.md`** (full spec) + a live interactive artifact where the signature moves actually run. **NO CODE CHANGED.** Awaiting Michael's green light.
- **The direction:** `user_credits.total_earned` is server-authoritative, un-mintable (service_role-only `increment_user_credits`, reachable only via `approve_task`, never touched by `purchase_reward`) — and both credit reads in the client select `'balance'` only, so it has never been rendered. **Balance is what you can spend; standing is what you are.** Spine = standing (5 bands, iron→brass→mode-accent). Grafts: BESKAR's chassis + material law, INK & SIGNAL's seal physics + central `shouldAnimate()` gate, RELAY's presence stripped to LIT/EMBER/COLD.
- **Verified-by-me findings (re-checked against the tree; agents got 5 claims wrong or overstated, incl. the "payday fires exactly once" claim that propagated verbatim across 4 directions — it fires twice, IssuedPage.tsx:212 + RewardsStorePage.tsx:115, both on the payer's device):**
  - `Layout.tsx:171` `bg-indigo-950` is opaque and covers the body starfield gradient on every signed-in screen; Layout early-returns a bare `<Outlet/>` when logged out, so **Login/Invite are the only routes that ever show the app's atmosphere**.
  - `.app-title{letter-spacing:.3em}` (index.css:75) is silently beaten by `.text-display{letter-spacing:.1em}` (:112) — same specificity, later wins, PageHeader applies both.
  - `--accent-blue:#00d4ff` hardcoded is what `.glass-card`/nav-active/focus actually consume → mode theming repaints ~15% of pixels. Two-line alias fix.
  - **5 dead deps, zero import sites:** animejs (+@types pinned ^3 against ^4), react-confetti, react-swipeable, react-currency-input-field, react-hook-form (+ deprecated @types/react-dropzone). Keep react-swipeable *only* if swipe-to-verdict is built.
  - `proof_description` collected/stored/rendered-by-nothing (text-only proofs show a blank dossier); `credit_transactions` written, never read; `in_progress` in the type union + transition map + Dashboard filter but written by no assignee UI (**no way to accept a contract**, while `soundManager` has registered an `acceptContract` key all along); streak subsystem complete with **two duplicate dead implementations**.
  - `click1e` requested by the History nav (Layout.tsx:158), registered nowhere, no file on disk → one of five tabs silently mute.
  - Perf, from the stack audit: `BaseCard` defaults to glass so **every card carries its own `backdrop-filter`**; 844 kb of 320 kbps MP3 fetched at first paint (`preload='auto'` at module scope, UI clicks are 2.09 s samples); Podfile iOS 14.0 vs Vite's Safari-16 default target = white-screen risk.
- **Deliberately killed** (judge-panel consensus): device-tilt parallax, scroll parallax, the 900 ms boot sequence, presence room labels, cold-relay decay, the weekly recap card, the who-did-more bar, press-and-hold verdicts, a second pull detent, 9 rank bands, diegetic renaming of the plumbing, three.js/GSAP/Rive/Lottie/vaul/Tailwind-v4-as-CSS-fix.
- **Open for Michael:** typical contract payout (sets the whole rank ladder), whether the one migration (`INSERT INTO credit_transactions` inside `approve_task`) lands now or later, whether family/couple modes are permanently gated, the iOS floor, `seal.mp3`, and a Mandalore glyph specimen check at 10/11/22px.

## 2026-07-15 — Deferred bug fixes shipped (c7da370), proposal 012 drafted (23ca5ec), Phase B client refactor on branch, Phase 5 iOS prep done
- **Context:** Michael green-lit the queued build items via voice note; he does the two-user proof/lifecycle browser test next chat. State verified on resume: 89baaba top, tsc 0, 82/82 tests, build clean. Both executor jobs ran via codex WRITE-mode; every diff centrally reviewed line-by-line before commit.
- **Deferred bug-hunt fixes SHIPPED (c7da370, pushed → Vercel deploys them):** (1) profileBootstrap now returns `{profile, error}` — AuthContext surfaces bootstrap failures as `profileError` instead of a silent null profile, and FTXGate never redirects to onboarding while profileError is set (offline existing user stays put); (2) checkFTXGate fails closed on query errors + useFTXGateLogic got a cancellation flag (stale gate result from a previous session can no longer clobber the new user's state); (3) Login.tsx fully i18n'd (28 keys under auth.login.*, en + natural du-form de) — only Supabase server error.message passthroughs and the AuthContext native-callback toast stay English (no imperative i18n.t pattern exists in src/; noted in-code). New src/lib/ftxGate.test.ts (fail-closed + both short-circuits). 85 tests.
- **Proposal 012 (Phase B) DRAFTED, zero SQL run (23ca5ec):** `db/proposals/012_create_update_task_rpcs.{md,up.sql,down.sql}` + `012_validation.sql` + `scripts/prod/{validate,apply,rollback}_012.ps1`. Design: `create_task` (server sets created_by/status) + `update_task(task_id, jsonb patch)` with a hard column whitelist — lifecycle columns unreachable by construction; drops the last 3 client write policies on tasks incl. the dead DELETE policy (delete_task RPC replaced it in 011). Open points A–C with recommendations (edit-gating parity, drop DELETE policy, no friendship check) await Michael. Policy names verified against schema.sql:754/810/831; live pre-flight = validation #3–#5.
- **Phase B client refactor DONE on branch `phase-b-client-rpcs` (f3927cb, local only, DO NOT DEPLOY before 012 SQL):** createTaskViaRpc/updateTaskViaRpc in domain/missions.ts reuse the 011 envelope parser; temporary Functions overlay in types/custom.ts (same 011 trick) until post-apply regen; useTasks + IssuedPage create/edit route through the RPCs and re-select with the existing profile joins; grep-audit: zero direct insert/update on tasks anywhere in src/. Bonus: updateTask now passes is_daily (was silently dropped before). 5 new tests → 90 total.
- **Phase 5 iOS prep (Windows-doable) DONE (23ca5ec):** capacitor.config.ts configured StatusBar/SplashScreen/Keyboard but none of the 3 plugins were installed — config was silently ignored; installed them, `npx cap sync ios` refreshed the stale hand-copied ios public/ (junk gone; marketing PNGs safe in assets-src/), Podfile lists all 5 plugin pods; `docs/runbooks/IOS_SHIP_RUNBOOK.md` = full Mac/TestFlight sequence + device smoke checklist + `bountyhunter://` redirect-allowlist caveat. Remaining Phase 5 needs a Mac.
- **npm audit fix (23ca5ec):** 3 prod advisories (react-router open-redirect GHSA-2j2x-hqr9-3h42, ws ×2) → 0 vulnerabilities.
- **Verification (mine, per commit):** tsc 0; vitest 15 files / 85 tests on main, 90 on the Phase B branch; build clean; lint 0 err / 3 pre-existing warnings; Phase B write-audit grep clean. | via codex (tasks mrkwq5q1, mrkx7j52)

## 2026-07-14 (later) — Codex bug-hunt: 15 findings, 11 verified + fixed (commit 4dc0ab7); 4 deferred
- **Context:** second half of the autonomous session. Codex read-only hunt over src/ (task-mrkprheg-z1rskx, ~9 min) returned 3 HIGH / 7 MED / 5 LOW; every finding was verified against source before acting. Fix implementation delegated back to codex WRITE-mode (task-mrkqg7qv-14dk9i) against an exact per-fix spec; diff centrally reviewed, one gap found + patched by hand.
- **Headline bug (HIGH, verified):** `bounty-proofs` is a private bucket (migration 20260611120000, public=false) but both upload paths stored `getPublicUrl()` links — **every file proof was unviewable by both participants**. Michael's pending lifecycle test would have hit this at "view submitted proof". Fix: stored URL format unchanged (delete/reject cleanup splits on '/bounty-proofs/'), new `ProofLink` component in TaskCard validates then exchanges the path for a 1-hour `createSignedUrl` (plain anchor, no async window.open — iOS popup blocker). Storage SELECT policy covers creator+assignee, so both sides can sign.
- **False-success UI (HIGH ×2 + MED):** IssuedPage.handleSubmitContract swallowed create/edit failures → TaskForm played success + closed, losing form data; archive handlers same pattern → modal closed on failure. All failure paths now throw; error toasted exactly once (page handler), form/modal stays open. Delete flow: `storage.remove()` `{error}` was ignored — deletion now aborts if proof cleanup fails (no orphaned private objects); cleanup stays BEFORE `delete_task` (RLS joins the tasks row).
- **Proof-object lifecycle (MED ×2):** failed `submit_proof` RPC now best-effort removes the just-uploaded file (both missions.uploadProof and useTasks.submitProof — the latter was the gap codex missed, patched centrally: timestamped paths orphan a new object per retry); successful reject now best-effort removes the old proof object (RPC clears proof fields, leaving the file unfindable otherwise).
- **Smaller (MED/LOW):** logout falls back to `signOut({scope:'local'})` when global signOut errors (R31 always-redirect kept); native appUrlOpen handler checks returned `{error}` from exchangeCodeForSession/setSession; locale keys added — common.loadingContracts (en/de), rewards.createModal.imageUrlError/imageUrlPlaceholder (en/de), languageSwitcher (de).
- **Deferred, documented:** profileBootstrap error-swallowing routes existing users into onboarding when offline (#7), ftxGate stale-request/session-switch races (#8/#10), Login-flow i18n bypass (#14) — need careful design and touch the exact flows Michael is mid-testing; queued as a build item.
- **Verification:** tsc 0, vitest 14 files/82 tests PASS, build clean 0 warnings, lint 0 err/3 warn. Runtime confirmation of signed proof links folds into Michael's two-user lifecycle test.

## 2026-07-14 — Build hygiene + docs refresh; codex bug-hunt in progress (commits fa0d3bf, 585591a)
- **Context:** Michael reported "domain verified" (which domain unclear — email-sending vs Vercel app domain; both next-step branches explained to him in-chat) and green-lit autonomous perf/polish/bug work. State verified on resume: e70da68 top, tsc 0, 82/82 tests, build green.
- **Build hygiene (fa0d3bf):** vite `manualChunks` splits react-vendor/supabase/i18n out of the main bundle — main chunk 722→388 kB (gzip 213→111 kB), chunk-size warning gone. `useRewardsStore`'s two dynamic `domain/rewards` imports made static (4 other hooks already import it statically, so the dynamic import split nothing — mixed-import warning gone). 5 double-encoded em dashes (mojibake) in `index.css` comments repaired via a node script (safe UTF-8 rewrite). The `/img/C1.jpg` warning was ALREADY fixed in an earlier pass — STATE/docs were stale on it.
- **Decision — route-level lazy loading SKIPPED:** small residual gain (~40 kB gzip) vs the stale-chunk-404-after-redeploy failure mode on a frequently-redeployed Vercel SPA; would need reload-on-chunk-error plumbing to be safe. Vendor split already delivers the cache win.
- **Docs refresh (585591a):** README "no npm test script" claim fixed (vitest suite + the tsc-doesn't-run-in-build warning documented); CODEX_NEXT_STEPS items 3 (proof types), 4 (lifecycle RPCs — Phase B remaining), 5 (types regen), 8 (build warnings) marked done with dates.
- **In flight:** codex read-only bug-hunt over all of src/ (races/auth edges/RPC mismatches/i18n/theme persistence/Capacitor hazards), job task-mrkprheg-z1rskx; findings triage + fixes next, then full checkpoint.
- **Verification:** build clean 0 warnings, tsc 0, vitest 82/82, lint 0 err/3 warn.

## 2026-07-11 (later) — Design pass: mode hero art in mission modal headers + bounty-board empty state (commit 5098679)
- **Context:** Michael gave design freedom via voice note: mission-modal redesign with mode hero art in the header, empty-state sword → bounty-board feel, use the codex image pipeline, "should be amazing", Mandalorian-bounty-hunter energy for guild but each mode gets its own world. This closed both parked design items from the FOR YOU list.
- **Modal hero headers (self):** `MissionModalShell` header now renders the existing mode hero key art (`hero-{guild,family,couple}.webp`, same 1024×683 set as onboarding step 1) full-bleed behind the role/title/context rows. Legibility handled by a dark bottom-weighted scrim (rgba(5,8,14) 0.40→0.92) + a faint mode-accent wash at the top + text shadows on role label/title/context; role-icon chip and StateChip got `backdrop-blur-sm`; per-mode `objectPosition` picks the focal slice (guild board 62%/30%, family chart 65%/28%, couple gift 50%/42% — only Y matters at banner aspect). State accent strip needed `z-10` (header became positioned, would have covered it). Zero extra vertical cost — art sits behind the existing rows. Heroes are 59–79 KB and already cached from onboarding.
- **Empty-state art (via codex, task-mrgh2fkj-xw0rsj):** `empty-missions.webp` regenerated — sword-in-campfire replaced by an empty weathered bounty board with torn parchment corner scraps under iron nails + two bare nails ("no contracts posted"), brass-gold engraving on black, true-alpha circular vignette, 512×512, 21.8 KB, corners alpha=0. Deliberately distinct from `empty-issued.webp` (blank scroll + quill = "write one"). Master kept at `assets-src/generated/empty-missions-board-v1.png`. Same filename → no code change needed. Visually verified in-chat before accepting.
- **Ops note:** the codex rescue subagent is fire-and-forget; its job state lives in `~/.claude/plugins/data/codex-openai-codex/state/<workspace-slug>/jobs/` and the main session polls that JSON directly (env `CLAUDE_PLUGIN_DATA` for the companion CLI; `status` without it can't see the job).
- **Verification:** tsc 0, build green (2.18s), 82/82 tests, committed + pushed 5098679 (Vercel will redeploy).
- **Open:** Michael to eyeball the modal header art + new empty state in browser; remaining empty states (chest/pedestal/horn/board+quill) kept — consistent set, only the sword was flagged. Email templates paste + realtime publication check explained to him in-chat this session.

## 2026-07-11 — Michael's first live two-browser test: theme-leak root-caused + hardened, invite/onboarding/login UX fixes
- **Context:** Michael ran the pending runtime test as two users on prod (partial: signup → invite link → friend accept → create mission worked; full lifecycle submit/approve still untested) and reported a batch of findings, headlined by a "mystery": the invite-link recipient's login page rendered YELLOW (family accent) though the sender is guild, then the new account showed family strings before flipping to guild. He also fixed Supabase auth Site URL/redirects in the dashboard himself mid-test (was still localhost:3000 → login bounced to localhost).
- **Root cause (codex investigation, task-mrehn3ap-44yur0, ~8min, findings verified against source):** the invite link carries nothing; the second browser had stale `bounty_theme='family'` in localStorage. Three real gaps let it survive: (1) onboarding step 1 preselects guild so pressing Next never calls setThemeId — nothing persisted; (2) "Skip setup" persisted no theme; (3) logout never clears `bounty_theme` OR `bounty_onboarding_completed`, so the next account on the device inherits both (the stale onboarding flag is also why laqy69 skipped onboarding). Profile adoption was additionally gated by a ref that trusts localStorage over the profile.
- **Hardening shipped:** shared policy in `themes.ts` (`PUBLIC_THEME_IDS=['guild']`, `isThemeId`, `toPublicThemeId`); `ThemeContext` rewritten — profile is authoritative, localStorage is only a cache, logged-out/fresh-account surfaces can only render public themes, logout/account-switch clears the cache, fresh accounts (profile.theme=null) get normalized to guild (cache + profile write); onboarding Next + Skip both persist explicitly; ProfileEditModal consumes the shared allowlist (dev still sees all 3); ftxGate localStorage flag now stores the completing user's id instead of 'true' (legacy 'true' values simply stop matching). Internal family/couple accounts keep working via profile.theme.
- **UX fixes shipped:** (1) Guild empty-state "Invite someone" now triggers the share-invite-link flow (was: invisible input focus — "does nothing"); (2) nav requests badge refreshes via a `bounty:friendships-changed` window event dispatched after every useFriends mutation (Layout's separate hook instance previously only updated via Supabase realtime, which likely isn't in the new project's publication — dashboard check parked); (3) onboarding wizard capped at max-w-2xl + step-1 hero banner responsive h-28→h-48 (art is 1024×683; full-width 112px strip looked broken on desktop); (4) onboarding step 3 REDESIGNED workflow-first per Michael's spec: 4 interactive lifecycle steps (hover/tap), active step reveals only its fields below (create→title/description, complete→assignTo/deadline, approve→proofRequired, earn→creditReward), mode-accent vars replace hardcoded teal; (5) Login gets a dedicated post-signup "Check your email" view (was toast-only, page looked dead); (6) MissionModalShell: no more fake "No further details provided" description box — empty description renders a centered reward column instead of a lopsided grid.
- **Also:** branded Supabase email templates written to `supabase/templates/{confirm-signup,magic-link}.html` (dark, guild-teal, table-layout inline-styles, `{{ .ConfirmationURL }}`) — Michael must paste them into Dashboard → Authentication → Emails.
- **Verification:** `tsc -p tsconfig.app.json --noEmit` = 0; `npm test` 14 files / **82 tests** PASS (3 new public-theme-policy tests; launchQuickFixes guard updated to point at the shared allowlist); `npm run build` PASS; lint 0 errors / 3 pre-existing warnings.
- **Open:** full post-011 lifecycle runtime test still pending; mission-modal full redesign = design direction wanted from Michael (empty-box fix shipped); empty-state artwork (sword → bounty-board feel) parked for an image-pipeline session; Supabase dashboard checks: realtime publication for `friendships`, email templates paste; login-page yellow-vs-teal question answered by the fix itself (public pages are always guild teal now).

## 2026-07-10 (latest) — Proposal 011 APPLIED to production live; types regenerated; deployed
- **Changed:** Michael pasted the live DB password in chat and asked to run the rollout + push everything. Ran the runbook exactly, with a review checkpoint between backup and apply (the permission gate itself required this — a chained blind backup+apply was denied as bypassing the mandated review step, correctly). (1) Pre-flight `validate_011.ps1`: no legacy trigger, both target policy names matched live exactly, old constraint confirmed `image/video` only, 0 rows would violate the new one — safe to proceed as-is, no up.sql edits needed. (2) `backup_schema.ps1` → `supabase\schema_backup_20260710_121149.sql`, verified 215 KB / 4981 lines, contains `tasks` table + `approve_task`. (3) `apply_011_up.ps1` → clean `BEGIN`...`COMMIT`, 5 functions created (submit_proof/reject_task/set_task_status/archive_task/delete_task), constraint now `image/video/document/text`, both broad assignee UPDATE policies dropped, creator-edit policy intact. (4) `git push` → 610773a, 2f8436f now on origin/main. (5) Post-validate confirmed all of the above live. **Finding, not a defect:** validation query #2 showed `anon` can also EXECUTE all 5 RPCs (not just `authenticated`, as the runbook's original success criteria assumed) — checked `approve_task`/`purchase_reward`/`mark_reward_redeemed` for comparison, all show the identical pattern. Root cause: Supabase's platform-level default privilege grant on new `public`-schema functions, which `REVOKE ALL FROM PUBLIC` doesn't undo. Harmless (every RPC null-checks `auth.uid()` first); corrected the runbook's stated expectation rather than treating it as a regression. (6) Post-apply cleanup: regenerated `src/types/database.ts` from live (confirmed UTF-8 with BOM, all 5 RPCs present with their real arg types), removed the temporary `DatabaseWithTaskLifecycleRpcs` overlay from `custom.ts` entirely (the 3 call sites + 1 test now use the plain `Database` type or the already-typed `supabase` client directly — several `as unknown as` casts became unnecessary and were removed). The regenerated types are stricter (`p_rejection_reason?: string`, no `| null`) than the temporary overlay was — fixed 6 resulting type errors at the 2 RPC call sites (`|| null` → `|| undefined` / `?? undefined`) and 1 test assertion.
- **Verification:** `tsc -p tsconfig.app.json --noEmit` = 0; `npm run build` PASS; `npm test` = 14 files / 79 tests PASS (post both the apply and the types-regen changes, run twice).
- **Security note:** flagged to Michael that the plaintext DB password is now in the chat transcript regardless of in-session handling; recommended rotating it in the Supabase dashboard as hygiene. Password was only ever held in `$env:PGPASSWORD` for the duration of each script call, cleared immediately after, never written to a file or echoed.
- **Open:** Runtime lifecycle testing (the actual core loop as two users) is still unverified in a browser — tsc/build/vitest confirm the code is correct, not that it behaves correctly end-to-end. Confirm the live Vercel deployment picked up the new commit.

## 2026-07-10 (later) — Proposal 011 APPROVED + finalized; codex refactor reviewed, delete-ordering bug fixed; rollout blocked only on DB password
- **Changed:** Michael approved 011 in chat (PDF proofs confirmed; open points B–D delegated). (1) **Decisions recorded in the proposal .md**: A = proof_type whitelist `image/video/document/text` ('pdf' literal removed — client maps PDF→'document'; 'url' excluded, never written); B = reject lands 'rejected'+reason as drafted; C = archive keeps either-party/any-status parity; D = IF EXISTS drops + pre-flight name check, post-apply MUST confirm policies gone. (2) **Recon correction folded into up.sql (new section 0.5)**: NO migration ever widened `tasks_proof_type_check` — every snapshot still has ('image','video'); proposal 010 only widened the storage bucket. up.sql now normalizes the constraint; validation #5b pre-checks existing rows. (3) **011 scripts created** (`scripts/prod/apply_011_up.ps1`, `validate_011.ps1`, `rollback_011.ps1`, 008 pattern + ON_ERROR_STOP) and `backup_schema.ps1` retargeted to mvbmpcmexkgfairnthux (old defaults pointed at the paused project). Runbook rewritten READY with the exact 5-command sequence. (4) **Client refactor delegated to codex** (entry below), diff reviewed centrally: one real bug found + fixed — codex cleaned up the proof Storage object AFTER `delete_task`, but the bounty-proofs delete policy joins `public.tasks` by folder task-id, so post-delete removal always fails RLS and orphans the file; reordered cleanup BEFORE the RPC in useTasks.deleteTask + IssuedPage.handleConfirmDeleteTask (local proof_url), amended up.sql/proposal comments. Codex's unauthorized protocol/memory edits reviewed: kept where accurate, corrected the post-delete-cleanup claims (ARCHITECTURE/DECISIONS/STATE), NEXT_STEP/BOARD rewritten by me.
- **Verification (mine, after the fix):** `tsc -p tsconfig.app.json --noEmit` = 0; `npm run build` PASS; `npm test` 14 files / 79 tests PASS; grep audit: only the two sanctioned creator-edit `tasks.update` sites remain.
- **Blocked:** DB password is not stored on this machine (no pgpass/env/keyring access) — correct hygiene, so the SQL apply is Michael's: PROD_RUNBOOK_011.md sequence (validate → backup → apply → git push → validate). Refactor commit stays UNPUSHED until then; Vercel redeploy of the OLD commit is safe meanwhile.

## 2026-07-10 — Task-lifecycle client writes moved to proposal-011 RPC contract (static-ready, SQL still unapplied) — *entry by codex executor; delete-cleanup claim superseded by the entry above*
- **Changed:** Replaced every in-scope direct `tasks` lifecycle write in `src/hooks/useTasks.ts`, `src/domain/missions.ts`, and `src/pages/IssuedPage.tsx` with `set_task_status`, `submit_proof`, `reject_task`, `archive_task`, or `delete_task`; `approve_task` and the two deliberate Phase-B creator-edit writes remain unchanged. Delete flows now consume the RPC-returned `proof_url` for post-delete Storage cleanup. Removed the obsolete 42703 retry around `rejection_reason`; the RPC is authoritative. Added the temporary five-function database overlay in `src/types/custom.ts`, centralized all nine JSON error-code mappings/idempotent-success handling, kept en/de parity for the new cleanup warning, and narrowed persisted proof types to image/video/document/text.
- **Tests:** Added `src/domain/missions.test.ts`: all nine logical errors, five idempotent flags, and the reject/status/text-proof/no-proof/archive RPC routes. Suite 59 → 79 tests (14 files).
- **Verification:** Static audit finds only the two explicitly preserved direct `tasks.update` sites (useTasks creator edit; IssuedPage creator edit). `npx tsc -p tsconfig.app.json --noEmit` = 0 errors; `npm run build` PASS; `npm test` = 14 files / 79 tests PASS. Runtime verification intentionally impossible because proposal-011 SQL is not live. No SQL, CLI, network, commit, or push.
- **Open:** Do not deploy this client build before applying/validating proposal 011. Production apply still needs backup + Michael's explicit go; afterward regenerate `database.ts`, remove the temporary RPC overlay, and browser-test lifecycle flows.

## 2026-07-10 — Apple-design pass applied + task-lifecycle RPC draft (proposal 011, zero SQL applied)
- **Changed:** Michael overrode the ask-gates for this session ("work through it to completion, don't ask") and pointed at the new global `apple-design` skill. (1) **Apple-design audit + implementation** (full writeup: `docs/premium-v1/APPLE_DESIGN_AUDIT.md`): press response fixed — buttons transitioned `:active` at 300ms (mushy); now `:active` overrides transition-duration to 0.08s (instant in, graceful settle out); `touch-action: manipulation` on all `.btn-*`/nav/modal-icon buttons (kills iOS double-tap delay); TaskCard (most-touched element) + Fab + RewardCard had NO press state → `motion-safe:active:scale` + `touch-manipulation` added. New motion tokens `--ease-enter` (iOS sheet curve 0.32,0.72,0,1) / `--ease-exit` (exact mirror) drive all modal animations; enter durations raised (slide-up 0.35s, scale-up 0.25s), exits unchanged so MissionModalShell's 200ms unmount timer stays valid. Reduced-motion completeness: modal fallback now cross-fades instead of freezing opaque; coin spins/scan-lines/laser-pulse/cursor-trail/sheen + all button transforms disabled under `prefers-reduced-motion`; new `prefers-reduced-transparency` block frosts glass (modal tokens solid, blur 0). MissionModalShell's hardcoded inline material now reads `--modal-bg`/`--modal-blur-*`/`--modal-border` tokens. tailwind.config.js duplicate `animation` key merged (`spin-slow` was silently dead). Deliberately NOT changed: haptics stay on commit/click (causality-correct), brand typography (Mandalore tracking), no spring library (nothing gesture-driven enough — parked: draggable sheet).
- **Changed (2):** **Task-lifecycle RPC DRAFT — proposal 011** (CODEX #4; Michael's "let's go" = draft, review before any SQL; **nothing applied anywhere**). Explore agent mapped every client lifecycle write first (two parallel paths: useTasks.ts AND missions.ts/IssuedPage.tsx). Draft: `db/proposals/011_task_lifecycle_rpcs.{md,up.sql,down.sql}` + `011_validation.sql` + `docs/runbooks/PROD_RUNBOOK_011.md` (DRAFT). Five SECURITY DEFINER RPCs mirroring approve_task v3 (FOR UPDATE lock, status preconditions, idempotent, JSON returns): submit_proof (folds in no-proof submit), reject_task (canonicalizes the divergent reject paths — useTasks reset to 'pending', missions.ts sets 'rejected'; canonical = 'rejected'+reason), set_task_status (pending⇄in_progress whitelist only), archive_task, delete_task (returns proof_url for client storage cleanup). Plus: DROP legacy `award_credits_on_completion` trigger (double-credit hazard vs approve_task's explicit credit — validation query checks live existence) and DROP the two broad assignee-side UPDATE policies (the actual security payoff — currently an assignee can write ANY column). create/update_task = Phase B follow-up. 4 open points flagged for Michael (proof_type whitelist vs live CHECK; reject semantics; archive gating; live policy names).
- **Verification:** `npm run build` ✓, `tsc -p tsconfig.app.json --noEmit` = 0 errors (baseline protected), lint 0 errors/3 pre-existing warnings, 59/59 tests. Visual eyeball of the design pass = Michael (blocked on Vercel env vars, below).
- **Found:** Michael reports the deployed site shows an **empty screen — missing Supabase env vars on Vercel**: the new project's `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` were never added to the Vercel project (values live in `.env.local`; printed for him in NEXT_STEP). Vite inlines env at build time → **must redeploy after saving them**. He says dashboard auth config (Site URL + redirects) is already done.
- **Open:** Michael: add the 2 Vercel env vars + redeploy, then eyeball Phases 1–4 + the new press-feel/motion pass; review proposal 011 (4 open points). Then: client refactor to the RPCs (after 011 approval) or Phase 5 ship vehicle.

## 2026-07-08 (late night, session 2) — DB types regenerated from the live project; tsc baseline 15 → 0 (fcb830d)
- **Changed:** Picked CODEX_NEXT_STEPS #5 (DB-types regen) over Phase 5 (cap sync/TestFlight needs a Mac + device). (1) Regenerated `src/types/database.ts` via `npx supabase gen types typescript --project-id mvbmpcmexkgfairnthux` (the `--linked` path demands SUPABASE_DB_PASSWORD; the `--project-id` path uses the CLI login and just works). New types gain daily_mission_streaks, invites, profiles.theme/onboarding_completed/partner_user_id, collected_rewards.redeemed_at, tasks.approved_at + the 5 post-restore RPCs; legacy marketplace_bounties/collected_bounties tables and create_bounty/purchase_bounty RPCs are GONE (grep-verified unreferenced in src — only `update_reward_store_item` still takes p_bounty_id, unchanged). File converted UTF-16LE → UTF-8 no-BOM (git diffed it as binary before). (2) `custom.ts` overlays that duplicated regenerated columns removed (Profile/CollectedRewardRow/Invite are now plain Row aliases). (3) Fallout + baseline burn-down to **tsc = 0 errors**: optimistic task gets `approved_at: null`; TaskForm's local NewTaskData gains `is_daily?`; PageHeader `Omit<…,'title'>`; PageQuote dead React import; missions.ts imports StatusChangeContext from contracts.types (was never re-exported by contracts.domain).
- **Found:** ⚠️ **Real bug surfaced by the regen: PDF/video proofs were blocked at runtime** — core `validateProofPayload` only knew text/url/image, so the 'document'/'video' types uploadProof passes fell to the "Unknown proof type" branch and failed validation before upload. The 2026-07-07 proof-types decision was wired in the UI/domain layer but never end-to-end tested (Supabase was paused). Fixed: ProofType widened to +video/document, file-required validation shared with image, new `src/core/proofs/proofs.domain.test.ts` (10 tests) locks it.
- **Verification:** `tsc -p tsconfig.app.json --noEmit` = **0 errors** (was 15); `npm run build` ✓; `npm run lint` 0 errors/3 pre-existing warnings; `npm test` 49 → **59/59**. Committed fcb830d.
- **Open:** Michael-gates unchanged (dashboard auth config; eyeball; sound audition). Next: Phase 5 ship vehicle, or task-lifecycle RPCs (CODEX #4). The tsc-clean baseline is worth protecting — consider adding the tsc line to CI/pre-commit later.

## 2026-07-08 (late night) — Phase 4 sound & haptics: unified feedback.ts committed (70bdff3)
- **Changed:** Picked Phase 4 over task-lifecycle RPCs (pure client work, no Michael-gate, roadmap order). (1) `@capacitor/haptics@8` installed; new **`src/utils/feedback.ts`** — semantic API (`tap`/`success`/`payday`/`warning`/`error`) firing sound + haptic together; haptics respect the existing sound toggle (mute silences vibration too); web falls back to `navigator.vibrate`/no-op. (2) AppButton + Fab fire a light impact on every tap; all 14 direct `soundManager.play` call sites routed through feedback: nav clicks (Layout), approve = success haptic + `approveProof` + `payday` (the old triple-play stacked success.mp3 twice), reject = warning haptic (was silent), delete = warning + delete sound (IssuedPage + useDeleteBounty), reward claim = payday (was silent), mark-redeemed + friend accept + saves = success. (3) soundManager volume pass: per-sound volumes (clicks 0.35, actions 0.5, coin/payday 0.65), Android-specific volume override dropped; new `payday` key aliases coin.mp3 (distinct sound parked for audio audition). (4) 5 new vitest specs guard the contract (44→49).
- **Verification:** build ✓, lint 0 errors/3 pre-existing warnings, test 49/49, tsc = 15 = baseline. **Browser smoke on the dev server (login page, pre-auth):** page renders clean; `feedback.tap()`+`payday()` exercised via console — no errors.
- **Found:** ⚠️ **A static `import '@capacitor/haptics'` white-screened the DEV app** (prod build fine): vite's optimizer mixed chunk generations (auth-helpers chunk kept an old `v=` hash) → two React copies → "Invalid hook call"/"Cannot read properties of null (reading 'useState')" in AuthProvider; survived `.vite` cache purge + hard reload. Fix: lazy `import()` of the plugin inside feedback.ts (also keeps @capacitor/* off the page-load critical path). Diagnosed by stashing the working tree (HEAD rendered fine → my diff, then bisected to the import).
- **Open:** Sound audition still needs Michael's ears (upload/toggle alias click files; payday aliases coin). Haptics need a real device (`npx cap sync ios` — Phase 5). Phase-4 leftover: none code-side. Next: task-lifecycle RPCs or DB-types regen per CODEX_NEXT_STEPS.

## 2026-07-08 (night) — Phase 2 committed; Phase 3 visual identity generated + wired + committed
- **Changed:** Michael's go ("commit it… phase three, let's go"). (1) Committed the Phase-2 working tree as **47e7eb7** (38 files; db_cluster-*.backup/ added to .gitignore first). (2) **Phase 3 end-to-end:** generated 16 art masters via parallel `codex:codex-rescue` agents (Codex CLI + gpt-image-2), post-processed with a System.Drawing pipeline (scratchpad `asset-pipeline.ps1`: magenta/green chroma-key with distance-feather + defringe, radial edge fade for empty-states, HQ resize) + ffmpeg → **17 WebP (566 KB) in `src/assets/generated/`**; masters committed to `assets-src/generated/`. (3) Wired via 5 reviewed sub-agents (2 Opus, 3 default), disjoint file ownership: Coin.tsx raster face (API/spin/text-overlay preserved); new `TypeEmblem.tsx` (per-mode gift medallions: guild teal/family honey/couple rose) replacing 🎁 at TaskCard + MissionModalShell; **type-based card accents** via new `getTypeAccentVariant` (credit→gold, gift→mode accent; random-hash accent stays on RewardCard only); dormant daily badge (Flame + `strings.dailyLabel`); onboarding hero banners (3 painterly heroes, gradient blend); `EmptyState.illustration` prop + 5 primary empty states (missions/issued/friends/store/archive); RewardCard default-🎁/broken-image fallback → 4 rotating still-lifes (hash per reward id; user-picked emoji still wins). (4) iOS: AppIcon 1024 = logo master; 3× 2732 splash composited on #100F10 (logo bg sampled to avoid seam). All committed as **37b3a4b**.
- **Verification:** central re-run after all agents: `npm run build` ✓, `npm run lint` 0 errors/3 pre-existing warnings, `npm test` 44/44, `tsc -p tsconfig.app.json` = 15 = baseline (0 net-new). Every generated image visually reviewed at full size; chroma cut-outs checked for fringe at 256px.
- **Found:** ⚠️ **Concurrent Codex image runs race on a shared output pool** — 3 of 15 parallel tasks copied another task's newest PNG (couple emblem got the couple hero; family emblem + friends empty got the archive pedestal). Fix: verify every returned image, regenerate serialized (one at a time). Also one hallucinated path (`D:\bounty-hunter-app` sans `\CODING`) and one exit-127 transient. Rose-pink art must key on GREEN #00FF00 (magenta collides); defringe rule per key color (B≤G for magenta, G≤max(R,B) for green) leaves gold edges intact.
- **Open:** Browser eyeball still blocked on dashboard auth config (Michael). Not wired (deliberate): `emblem-credit.webp` (coin already carries credit identity; pouch reserved for a future payday/streak moment), lucide `Coins` at ArchivePage/OnboardingStep4, streak-flame count (needs useDailyMissionStreak wiring + product call), mode heroes in modal headers, is_daily create-form toggle. Phase 4 (sound/haptics) or task-lifecycle RPCs next.

## 2026-07-08 (later) — 9 migrations applied to the new DB + Phase 2 leftovers shipped (invite links, persistence, mark-redeemed, orphan surface)
- **Changed:** Michael gave the go ("apply the migration and all else"). (1) **Applied the queued 6-migration batch** to the new test DB (mvbmpcmexkgfairnthux) via the IPv4 session pooler with psql — 20260412100100/100200/100300 (credit lockdown), 20260611120000 (storage buckets/policies), 20260707220000 (rejection_reason), 20260707221000 (PDF proofs). Backup taken first (schema+data pg_dump to scratchpad). (2) **Wrote + applied 3 NEW migrations** for the Phase 2 leftovers: `20260708120000` profiles.theme + onboarding_completed; `20260708120100` collected_rewards.redeemed_at + `mark_reward_redeemed(uuid,bool)` RPC; `20260708120200` invites table (owner-RLS) + `get_or_create_invite()` / `redeem_invite(text)` RPCs (redeem creates an ACCEPTED friendship, dedup either ordering). Second backup before this batch. Recorded all 9 in supabase_migrations.schema_migrations (tracker 1→10 entries). (3) **Code wiring via 3 sequential sub-agents, every diff reviewed centrally:** Phase 2.6 mode/onboarding persistence (ThemeContext + ftxGate persist to profile, fire-and-forget, localStorage stays immediate truth); Phase 2.7 deleted the orphan `/profile/edit` page (dropped its URL-exposed debug panel + un-gated mode list), folded "Restart Onboarding" into ProfileEditModal; Phase 2.8 collected-rewards "mark redeemed/undo" (domain fn + hook + RewardCard toggle + toasts); Phase 2.5 shareable invite links (new `/invite/:token` PUBLIC route + InvitePage, useInvite hook, useRedeemPendingInvite post-login round-trip via localStorage stash, Share buttons on Friends both paths + onboarding invite step). DB types added via `src/types/custom.ts` overlay (Profile.theme/onboarding_completed, CollectedRewardRow, Invite) — the UTF-16 `database.ts` left untouched.
- **Files:** 22 modified / 6 new (3 migrations, src/hooks/useInvite.ts, src/pages/InvitePage.tsx, src/domain/rewards.test.ts); 1 deleted (src/pages/ProfileEdit.tsx). i18n en+de parity for every new key (invite.*, profile.restartOnboarding*, rewards.*redeem*).
- **Verification:** `npm run build` ✓, `npm run lint` 0 errors / 3 pre-existing warnings, `npm test` 41→44 (+3 markRewardRedeemed parsing tests). **Caught a masked regression:** `npm run build` does NOT typecheck pages (root tsconfig `files: []`), so the Round-2 agent's green build hid 7 real type errors in useCollectedRewards.ts (adding `redeemed_at` to the typed `.select()` poisons the row type via SelectQueryError). Fixed with `.returns<CollectedRewardRow[]>()` + non-optional field + typed map callback. Final `tsc -p tsconfig.app.json` = 15 errors, ALL pre-existing (streaks/daily_mission_streaks/PageHeader/missions per roadmap) — 0 net new. Migration application verified live: increment RPC + user_credits writes revoked from anon/authenticated, rejection_reason present, bounty-proofs allows application/pdf, invites RLS on, 3 new RPCs granted to authenticated (auth.uid() guarded so anon-execute is harmless, matches purchase_reward convention).
- **Found:** Pre-existing i18n drift (languageSwitcher.* missing in de, friends.description missing in en) — left as out-of-scope. Post-login invite redemption for a brand-new signup fires after onboarding (useRedeemPendingInvite lives in Layout, behind FTXGate); token persists in localStorage until then, so it still redeems — acceptable for V1. NOT committed (awaiting Michael's go).
- **Open:** Michael: (a) set the new project's dashboard auth config (Site URL + redirect URLs for magic links, localhost:6075 / Vercel) so login + invite round-trip are testable; (b) browser eyeball of everything; (c) go/no-go to commit this working tree. Remaining Phase 2 is now DONE — next is Phase 3 generated assets per docs/premium-v1/ROADMAP.md.

## 2026-07-08 — Supabase revived: fresh project (Mumbai) restored from Jan-2026 backup, data wiped
- **Changed:** Restored the old cluster backup (`db_cluster-28-01-2026@04-29-25.backup`, PG 15.14 pg_dumpall, 9157 lines) into Michael's NEW Supabase project `bounty-hunter-app` (ref mvbmpcmexkgfairnthux, ap-south-1 Mumbai) — one sub-agent executed, orchestrator verified independently. Pre-flight: no role PASSWORD clauses; template1 section trimmed. Direct DB host is IPv6-only and this machine has no IPv6 route → used the Supavisor session pooler `aws-1-ap-south-1.pooler.supabase.com:5432` (user `postgres.mvbmpcmexkgfairnthux`); region discovered via `supabase projects list`. Restore produced 473 errors, all classified benign (already-exists roles/schemas, permission-denied on supabase-managed objects, realtime partition drift, vault.secrets COPY denied). Then wiped all data per Michael's explicit instruction: TRUNCATE 8 public tables RESTART IDENTITY CASCADE, DELETE 9 auth.users (identities cascaded), storage.objects already 0; 3 buckets (avatars, bounty-proofs, reward-images) kept. `.env.local` switched to the new URL + `sb_publishable_…` key; `supabase link` relinked.
- **Verification (independent):** 8 public tables at 0 rows, 3 buckets, 0 auth.users, 23 RLS policies, 14 public functions; `GET /auth/v1/health` = 200 with the publishable key. Restore log: session scratchpad `restore.log`.
- **Found:** ⚠️ **Restored schema is PRE-hardening** — `increment_user_credits` EXECUTE-granted to anon+authenticated and `user_credits` fully writable by clients (the exact top-risk the April 2026 migrations fixed); `supabase_migrations.schema_migrations` has only ONE entry (20231117000000) → old prod was managed via SQL editor, tracker useless for reconciliation. Repo migrations 20260412100100/100200/100300, 20260611120000, 20260707220000, 20260707221000 all need (re-)applying — blocked on Michael's go per the prod-SQL rule (auto-mode classifier also enforces this). `rejection_reason` column absent (expected); `proof_type` present. Edge functions and Vault secrets did not transfer (not in a DB dump / permission-denied). Old paused project still holds the only copy of real user data.
- **Open:** Michael: go/no-go for the 6-migration batch; dashboard auth config for the new project (Site URL + redirect URLs for magic links, port 6075 / Vercel); then browser eyeball of Phase 1+2 UI. Mumbai region = latency from Europe; fine for tests.

## 2026-07-07 (late night) — Phase 2 UX coherence: noun system, realtime badges, History nav, rejection loop, PDF proofs
- **Changed:** Michael decided the two parked product calls: noun system = Mission (guild) / Chore (family) / Request (couple), store items = plain Rewards, "Bounty" reserved for a mission's credit pot; proof types = PDF/text/private all allowed. Orchestrated 4 sub-agents (2 Opus, 2 Sonnet) in two waves, every diff reviewed centrally. Wave 1: (a) tasks realtime wired into useAssignedContracts/useIssuedContracts via new shared `useTasksRealtime` (debounced refetch, channel-ref guard + monotonic channel-name counter — same hardening as the UserCredits white-screen fix) + nav action badges via new count-only `useActionCounts` (review count on /issued, rejected count on /) + History nav re-enabled (route/page already worked); (b) noun-system i18n sweep en+de: new `taskStatus.*` chip keys (TaskCard hardcoded English gone), contracts/taskForm noun-neutral or `{{noun}}`-interpolated (components pass capitalized `strings.missionSingular`), rewards/bounties "Kopfgeld"→"Belohnung" sweep, `theme.guild.rewardSingular` bounty→reward (+ themes.ts fallback), dead `navigation.*`+`bounties.*` namespaces deleted, missing de `rewards.editModal` block + `contracts.createFailedMessage` key added (both latent bugs). Wave 2: (c) rejection loop — `rejectMission` now persists status='rejected' (was: silent reset to 'pending') + optional `rejection_reason`, 42703-tolerant (retries without the column until the migration is applied); reject-with-reason ModalShell dialog on IssuedPage (300-char textarea); TaskCard shows the reason to the assignee + resubmit path (uploadProof clears the reason, transition matrix rejected→review/completed added, +5 domain tests); (d) proof types — PDF+video in dropzone allowlist, uploadProof accepts PDF as proof_type 'document' (ProofType union widened), bucket migration adds application/pdf.
- **Files:** 22 changed / 8 new — key new: src/hooks/useTasksRealtime.ts, useActionCounts.ts, src/lib/debounce.ts(+test), supabase/migrations/20260707220000_add_rejection_reason.sql + 20260707221000_allow_pdf_proofs.sql (NOT applied — Supabase paused), db/proposals/009+010, docs/runbooks/SUPABASE_RESTORE_CHECKLIST.md.
- **Verification:** `npm run build` ✓, `npm run lint` 0 errors/3 pre-existing warnings, `npm test` 41/41 (33→41: +3 debounce, +5 rejection transitions). Realtime delivery NOT live-testable (Supabase paused). src/types/database.ts UTF-16LE encoding verified preserved.
- **Found:** Supabase project is PAUSED — login dead ("Failed to fetch"); no base-schema migration exists in the repo, the schema lives only in the paused project → restore checklist written (backup download first!). Private proof needs no schema work — storage RLS already limits proofs to creator+assignee. Register mix in de locale: contracts/taskForm blocks are Sie-form, rewards.empty now du-form — flag for a future one-direction sweep.
- **Open:** Michael: restore Supabase (both new migrations apply at restore), then eyeball Phase 1 glass-card surfaces + wave 1/2 UI. Remaining Phase 2: invite links (needs live Supabase), mode/onboarding persistence (prod SQL), orphan surfaces, collected-rewards loop. Phase 3 assets untouched.

## 2026-07-07 (night) — Phase 1 congruence COMPLETE: TabBar, ModalShell, accent single-source, i18n sweep
- **Changed:** Orchestrated 4 sub-agents (2 Sonnet, 1 Codex, 1 Opus) in two waves; reviewed every diff centrally. (1) Shared `TabBar` primitive (role=tablist/tab, aria-selected, roving tabIndex + arrow keys, count badges, unread dot, fullWidth variant) — Friends + RewardsStore migrated, drift resolved toward the glass aesthetic. (2) `src/theme/modeAccents.ts` is now the single TS source for mode accent hexes — modalTheme/ProfileEditModal/OnboardingStep1Mode import it; index.css keeps CSS vars with SYNC NOTE comments + 2 new drift-guard tests (suite 31→33). No drift existed between the 4 old copies. (3) i18n sweep: ~26 hardcoded strings → keys in en+de (PageState defaults, IssuedPage toasts/empty states/Fab, ArchivePage stats, UserCredits, de `rewards.confirmDialog` block added); tablist aria-labels wired. (4) Generic `ModalShell` primitive (portal, one backdrop recipe bg-black/70+blur, glass-card + modal-enter surface, Escape, UIContext modal layer, 44px close, dialog a11y, sheet/tall variants) — CreateBounty/EditBounty/TaskForm/ProofModal/ProfileEditModal migrated; TaskCard's redundant double-portal of ProofModal removed. Net −151 lines.
- **Files:** 19 changed / 3 new (src/components/ui/TabBar.tsx, ModalShell.tsx; src/theme/modeAccents.ts)
- **Verification:** `npm run build` ✓, `npm run lint` 0 errors/3 pre-existing warnings, `npm test` 33/33.
- **Found:** Intentional visual normalizations to eyeball in-browser: Create/EditBounty + ProfileEdit surfaces are now translucent glass-card (were solid gray-900); ProfileEdit backdrop /80→/70; RewardsStore tab border/text-size normalized; TaskForm/ProofModal close button moved into the surface corner. Backdrop close is now text-selection-safe everywhere. ConfirmModal deliberately NOT re-based on ModalShell (critical overlay layer + capture-phase Escape are out of shell scope). Backlog strings still hardcoded: "Submit Proof" (ProofModal), "Uploading..." (Create/EditBounty), FileUpload size error, "New User" (ProfileEditModal), MissionModalShell internals (Reward/Overdue/Delete mission/Move to History...).
- **Open:** Phase 2 UX coherence next (noun decision still parked for Michael) or Phase 3 assets.

## 2026-07-07 (late evening) - Gift emblem pilot asset generated
- **Changed:** Used the gpt-image-2 prompt skill + image generation flow to create one gift/present contract emblem pilot. Copied the generated PNG into `assets-src/generated/gift-emblem-pilot-v1.png`, then normalized the connected background field to exact solid `#FF00FF` for chroma-key use.
- **Files:** assets-src/generated/gift-emblem-pilot-v1.png
- **Verification:** PNG exists at the target path; 2,220,729 bytes; valid PNG signature; 1254x1254 square; sampled canvas edges are exact `#FF00FF`.
- **Open:** Remaining Phase 3 asset slots still pending: credit emblem, mode identity artwork, empty-state illustrations, reward-store placeholder art, iOS icon/splash.

## 2026-07-07 (evening) — Refactor landed; 4-track audit; premium polish phase 0-1
- **Changed:** Verified + committed the floating UI refactor (fixed 2 broken theme-contract tests first). Ran a 4-agent audit (design congruence / code review / UX flow / assets+sound). Fixed two white-screen P1s the audit found in the fresh refactor (emoji names crash avatarFallback; duplicate UserCredits realtime channel subscribe throws and unmounts the app). Batch A polish (dead fonts, safe areas, Spinner primitive, `.modal-enter`, `.text-sm` weight bug, sound-registry fix — 10 sound keys were silently no-oping). Asset slim-down: public/ went ~71 MB → ~2 MB (marketing/raw audio/unused fonts → assets-src/; 1 MB logo → 32 KB + 3 KB favicon). Batch B+C (2 agents): every button → AppButton, every modal → modal-enter/Escape/44px/one backdrop, PageState/EmptyState/Spinner everywhere, teal → var(--mode-accent).
- **Files:** ~45 across src/, index.html, public/→assets-src/, docs/premium-v1/ROADMAP.md
- **Commits:** 8a18540, b17469b, 2c569a3, e5543dc, b0d5a61, 3e3191d
- **Found:** UX audit: 5 nouns for the task object; Archive/History page unreachable (nav commented out); /profile/edit orphan leaks all modes + debug panel; rejected proofs vanish silently; no action badges; invites only find existing accounts; theme/onboarding localStorage-only (profile.theme is read but never written). Asset audit: coin is SVG with clean slots; is_daily/streaks have full theme strings but zero UI; no haptics. Image pipeline: Codex CLI 0.128.0 + gpt-image-2-skill (~/.codex/skills/). chrome-devtools MCP deadlocks with parallel Claude sessions (shared profile — needs --isolated).
- **Open:** dead-CSS purge + Codex image pilot running as background agents at write time; roadmap = docs/premium-v1/ROADMAP.md (5 phases); noun-system decision parked for Michael.

## 2026-07-07 — Second Brain installed; project surveyed from evidence
- **Changed:** Created `memory/` (INDEX, STATE, DECISIONS, ARCHITECTURE, LOG + raw/notes/archive) and wired the Project Memory block into new AGENTS.md/CLAUDE.md. No app code touched.
- **Files:** memory/*, AGENTS.md, CLAUDE.md
- **Commits:** none (working tree already carried an unrelated uncommitted UI refactor — left untouched)
- **Found:** Last commit 2026-06-18 "Harden V1 launch readiness" (~3 weeks idle). History: Oct 2025 doc deep-dive → security proposals 001–008 with prod runbooks → Jan 2026 onboarding/tutorial/themes burst → June 2026 codex refactor pass (domain extraction, vitest suite, credit lockdown, storage-bucket migration, fresh docs). Working tree holds an uncommitted UI-primitives consolidation (27 files, +625/−906).
- **Open:** Production migration state unknown; UI refactor unverified/uncommitted; proof-type product decision pending — see [[STATE]] Open questions.
