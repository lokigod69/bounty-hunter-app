# NEXT STEP — Bounty Hunter/main — updated 2026-07-11 (design pass shipped: modal hero headers + bounty-board empty state)

## FOR YOU
1. **Paste the email templates** — Supabase Dashboard → Authentication → Emails → **Templates tab** (NOT "SMTP Settings" — you don't need SMTP/sender config for this). Select template "Confirm signup" → replace the message body with the full contents of `supabase/templates/confirm-signup.html`, set subject to "Confirm your email — Bounty Hunter", Save. Then select "Magic Link" → paste `supabase/templates/magic-link.html`, subject "Your sign-in link — Bounty Hunter", Save. The other templates (Invite user, Change email, Reset password) stay default for now.
2. **Check realtime publication** — Dashboard → Database → Publications → click `supabase_realtime` → is the `friendships` table toggled on? If not, toggle it on (add `tasks` too while you're there). Why: when your friend accepts a request in THEIR browser, YOUR nav badge only updates live if Postgres broadcasts changes on that table; same-browser updates are already fixed in code.
3. **Browser re-test after Vercel picks up commit 5098679**: open any mission card → the modal header should now show the guild hero art behind the title; empty mission inbox should show the new empty bounty board (sword is gone). Plus the previous batch: onboarding step 1 art + step 3 hover-reveal, signup "Check your email" view, invite button, requests badge, always-teal logged-out pages.
4. **Finish the post-011 core loop test** as two users: submit proof → reject with reason → resubmit → approve (credits land once) → archive → delete.
5. Still parked: sound audition; rotate the DB password (pasted in chat 2026-07-10); design taste-call on whether the remaining empty-state art (chest/pedestal/horn) also gets refreshed.

## PASTE THIS
Resume Bounty Hunter, workstream main, under protocol-os.
Read protocol/PROTOCOL.md, protocol/NEXT_STEP.md (this file), memory/INDEX.md, memory/STATE.md.
Verify state: `git log --oneline -3` — expect 5098679 "Design pass: mode hero art in mission modal headers + bounty-board empty state" on top of bae7d14. `npx tsc -p tsconfig.app.json --noEmit` = 0 (protect; build does NOT typecheck pages); `npm run build` green; `npm test` green (14 files / 82 tests). Supabase LIVE on mvbmpcmexkgfairnthux (ap-south-1).
DONE 2026-07-11 (later): both parked design items shipped — MissionModalShell headers render mode hero art (hero-guild/family/couple.webp) full-bleed behind role/title/context with dark scrim + accent wash + text shadows + per-mode objectPosition (state strip z-10 fix); empty-missions.webp regenerated via codex image pipeline (sword → empty bounty board, torn scraps under nails, gold engraving, true alpha, 512×512; master in assets-src/generated/). Codex job state readable at ~/.claude/plugins/data/codex-openai-codex/state/<slug>/jobs/ when the rescue wrapper returns fire-and-forget.
PENDING MICHAEL: see FOR YOU above (email templates via Templates tab, realtime publication, browser re-test incl. new art, finish lifecycle test, sound audition, password rotation).
NEXT BUILD ITEM (either, no blocker): Phase B (create_task/update_task RPCs — drops the remaining creator-side client write policy) or Phase 5 ship vehicle (cap sync ios, device safe-areas, TestFlight — needs a Mac).
Mode 2 rules apply. One LOG line per step; checkpoint per Iron Rule 3; end checkpoints with the Status Block.
