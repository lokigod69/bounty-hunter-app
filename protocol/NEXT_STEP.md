# NEXT STEP — Bounty Hunter/main — updated 2026-07-29 (walkthrough feedback shipped; 013 drafted; 12-language i18n)

## FOR YOU
1. **Three decisions, all with recommendations — the only things blocking work:**
   - **Proposal 013 go/no-go** (`db/proposals/013_standing_self_assign.md`, open points A–E). Closes the self-assign standing hole. Low risk: three function-body swaps, no signatures/policies/schema, so no types regen and no deploy-ordering window. **Answer E first** — a self-assigned contract exists on the live DB (`tesatmynutes`, you as both creator and assignee) although the assignee dropdown only offers friends, so some other path creates them; validation query #7 finds it.
   - **Rank ladder recalibration.** You asked what a typical payout is; the answer was already in the code. `standing.domain.ts` thresholds (0/120/600/2000/8000) assume ~20 credits per contract in their own comment, but the create form offers **1/2/3/5/10** and calls 2 a "small chore". At a realistic ~3 credits, band 3 is ~667 contracts and band 4 ~2,667 — nobody reaches them. Recommend dividing the table by ~4 → **0/30/150/600/2000**. One-line change; it retroactively promotes whatever rank you have now, which is why it is your call.
   - **German "Sie" vs "du".** The ten new locales use informal address (this is a family/couples app). German currently uses formal "Sie". Recommend switching it to match, but that rewrites ~410 existing strings, so I have not touched it.
2. **Browser-test one create + one edit mission.** Still the only unverified part of the 012 path (needs a real `auth.uid()` round-trip). Everything else about 012 is confirmed live.
3. **Try the language picker** — twelve languages are live (Espanol, Portugues, Italiano, Francais, Romana, Nederlands, Polski, Svenska, Dansk, Cestina). Caveat worth knowing before you judge them: ~150 strings are still hard-coded English in components (the whole onboarding flow, the reward create/edit modals, friend cards), so those show English in every language until the extraction pass. What IS translated is the contract/mission surface, standing, history, and the store.
4. **Eyeball the five design fixes** from your walkthrough: modal backdrops are now properly opaque and consistent; a contract with **no** description centres its reward instead of leaving an empty box on the left; description and reward panels are always the same height; the German header no longer collides (the rank word hides below 1280px and the sigil carries standing); the one-option Mode picker is gone from both the profile and onboarding.
5. **Nothing needed on the DB password** — recorded as deliberately not rotating, per your call.

## PASTE THIS
Resume Bounty Hunter, workstream main, under protocol-os.
Read protocol/PROTOCOL.md, protocol/NEXT_STEP.md, memory/INDEX.md, memory/STATE.md.
Verify state: main at `9620413` or later. Note the 2026-07-29 commits may still be LOCAL — check `git status` and `git log origin/main..HEAD` before assuming they are pushed. Run `git diff --check`, `npx tsc -p tsconfig.app.json --noEmit`, `npm test`, `npm run lint`, `npm run build` (expect: 0 errors, 127+ tests/19+ files, 0 lint errors/3 warnings, build pass and warning-free).
DONE 2026-07-29: DB password rotation CANCELLED by Michael (risk accepted, recorded). **Proposal 013 drafted + proven against a throwaway local Postgres — 20/20 assertions, incl. a pre-existing self-assigned credit row completing with credited=false and total_earned unchanged; rollback and re-apply both verified (24a7613, UNAPPLIED, Michael-gated).** Five design fixes from Michael's browser walkthrough (7831ff9): one `--modal-scrim` token replacing five hard-coded backdrops; mission-modal empty-column root cause found (`children` is a JSX array, so it was always truthy and the two-column branch always won); equal-height description/reward panels; German header collision fixed width-robustly (rank word only at xl+); single-option Mode selectors hidden. **Twelve locales now live and verified (565e565):** en/de + es/pt/it/fr/ro + nl/pl/sv/da/cs, generated by two Codex waves and checked centrally — 417/417 key parity each, placeholders preserved, no BOM/U+FFFD (Codex writes JSON via PowerShell, the UTF-8 corruption path, so this mattered), 96-99% of leaves differ from English with zero long strings copied verbatim, rank words all <=13 chars and now pinned by a test. Main bundle 392 kB with all twelve vs ~590 kB if static. **i18n rearchitected to make that affordable (9620413):** lazy locale chunks (main bundle 407.8→390.5 kB), `src/i18n/languages.ts` registry, `i18next-browser-languagedetector` dropped, picker offers only filesystem-present locales, 17 new gate tests which immediately caught a duplicate `contracts.reject` in en+de and de's orphan `friends.description`.
NEXT: Michael's three decisions above (013, rank ladder, German register). Then the **mobile pass** — Codex's adversarial audit found 1 BLOCKER (landscape phones get the full desktop header, hamburger hidden, controls overlapping) and ~10 MAJORs (iOS keyboard covers modal submits — `vh` should be `dvh`; `.input-field` drops to 14px in landscape causing iOS focus zoom; `.safe-top` replaces rather than adds to `pt-16`; doubled page gutters leave ~296px usable at 360px; reward grid starts at `grid-cols-2` and clips actions; German tab bar's first tab cannot be scrolled into view). Full list in memory/STATE.md Known problems. Then the ~150-string i18n extraction (whole onboarding flow has no namespace at all) — this must land in `en` BEFORE the new locales can be completed — then a second translation pass over the added keys. Mode 2 rules and the Status Block apply.
