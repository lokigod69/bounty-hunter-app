# NEXT STEP — Bounty Hunter/main — updated 2026-07-24 (Design V2 Wave C implemented; browser smoke + commit next)

## FOR YOU
1. **Browser/device smoke Waves B+C as two users:** accept/transmit/approve seals land over the pressed control; sent-back lifts in orange without a shock; only the hunter hears/feels payday and sees one coin/count refresh; reload/login never replays old payouts. Confirm lifetime earned, Login art/Google surface, and English/German creed lines.
2. **Regression smoke:** text/image/video/PDF evidence both sides; issuer Sent back section; archive from either side/no Overdue; populated dossier survives refresh; nested Escape/focus still behave.
3. If that passes, commit the intentionally uncommitted Wave C tree. Proposal 012 branch remains deploy-locked until its SQL rollout; the older templates/realtime/Mac tasks remain listed on BOARD.

## PASTE THIS
Resume Bounty Hunter, workstream main, under protocol-os.
Read protocol/PROTOCOL.md, protocol/NEXT_STEP.md, memory/INDEX.md, memory/STATE.md.
Verify state: main at `ad447b4` (Wave B committed) plus an uncommitted Design V2 Wave C tree; preserve the pre-existing untracked `docs/design-v2/` register. Protected Phase-B files and Issued create/update block remain untouched. Run `git diff --check`, `npx tsc -p tsconfig.app.json --noEmit`, `npm test`, `npm run lint`, and `npm run build`.
DONE Wave B 2026-07-24: sent-back truth/section, Accept contract, EvidencePanel, SWR lists, modal a11y/LIFO/material, complete archive, truth fixes — committed `ad447b4`.
DONE Wave C 2026-07-24: anchored strike/lift seals; hunter-addressed baseline payout watcher + one-coin header drop + credits refresh; lifetime earned; guild Login/Google surface; bilingual seven-line creed, generic quote corpus removed. Evidence: tsc 0; Vitest 17 files/93 tests; lint 0 errors/3 pre-existing warnings; build pass.
NEXT: Michael browser/device smoke, then intentional Wave C commit. Do not merge/push `phase-b-client-rpcs` before proposal 012 SQL applies. Mode 2 rules and the Status Block apply.
